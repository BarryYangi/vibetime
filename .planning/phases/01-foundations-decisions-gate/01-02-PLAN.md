---
phase: 01-foundations-decisions-gate
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - .planning/DECISIONS.md
autonomous: false
requirements:
  - PROC-01

must_haves:
  truths:
    - ".planning/DECISIONS.md exists at the repo root and is committed to git"
    - "DECISIONS.md captures every PRD §16 delegated decision: monorepo tool, TS strategy, lint/format, test runner, zero-dep enforcement, per-package directory layout, git URL parsing approach, IPC method signatures, animation library, packaging tool, ad-hoc signing scripting, test strategy"
    - "Decisions resolved in Phase 1 are marked status: proposed (awaiting user approval) with full rationale, alternatives considered, and risk notes"
    - "Decisions deferred to later phases are marked TBD by Phase X with the explicit phase number (X = 4, 5, or 6) and a one-sentence reason"
    - "Each open assumption A1..A8 from RESEARCH.md is surfaced as an explicit row the user can approve or override"
    - "The plan terminates with an explicit STOP — workflow halts and waits for user 'approved' before any downstream plan executes"
  artifacts:
    - path: ".planning/DECISIONS.md"
      provides: "PRD §16 process gate artifact"
      min_lines: 80
      contains: "D-MONO"
  key_links:
    - from: ".planning/DECISIONS.md"
      to: ".planning/phases/01-foundations-decisions-gate/01-RESEARCH.md"
      via: "rationale columns reference research §A..§G"
      pattern: "RESEARCH"
    - from: "Plan 03 (core library code)"
      to: ".planning/DECISIONS.md user approval"
      via: "depends_on: [02] in Plan 03 frontmatter"
      pattern: "depends_on"
---

<objective>
Author `.planning/DECISIONS.md` capturing every PRD §16 delegated decision (resolved or explicitly deferred), present it to the user, and STOP — wait for explicit user approval before any downstream plan executes. This plan IS the §16 gate. Its terminal task is a non-negotiable checkpoint; without it, PROC-01 cannot be satisfied and the entire roadmap is blocked.

Purpose: Satisfy PROC-01 (DECISIONS.md exists, captures every delegated decision, user-approved before any implementation begins). The PRD treats §16 as the most important process control in the 5-week budget — every later phase carries assumptions that must be relitigated here, once, in full daylight.

Output: A committed `.planning/DECISIONS.md` plus an explicit pause for user review. No code is written in this plan. Plan 03 cannot execute until the user replies "approved" (or supplies overrides).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/intel/decisions.md
@.planning/intel/constraints.md
@.planning/phases/01-foundations-decisions-gate/01-RESEARCH.md
@.planning/phases/01-foundations-decisions-gate/01-01-SUMMARY.md
@vibetime-prd.md

<interfaces>
<!-- Plan 02 produces a single document. No code interfaces. -->
<!-- The "interface" is the DECISIONS.md schema below. -->

DECISIONS.md row schema (every row MUST follow this format):

| ID | Decision | Choice | Rationale (1 sentence) | Decided in Phase | Status |

Status values:
- `proposed` — Phase 1 recommends; awaiting user approval in this gate
- `deferred` — explicitly TBD; resolved in named later phase
- `locked` — already locked by PRD §5 (replicated here for traceability only)
- `approved` — user has confirmed (set retroactively after the gate clears)

Required IDs — sixteen total (every one MUST appear; missing any voids PROC-01):
- D-MONO, D-TS, D-TS-STRICTNESS, D-LINT, D-TEST, D-DEPCHECK, D-LAYOUT, D-GITPARSE, D-GITLAB-SUBGROUP, D-CI, D-PRECOMMIT  (Phase 1 — proposed)
- D-IPC                                                                                                                  (Phase 4 — deferred)
- D-ANIM                                                                                                                 (Phase 5 — deferred)
- D-PKG, D-SIGN                                                                                                          (Phase 6 — deferred)
- D-TEST-HOOK                                                                                                            (Phase 3 — deferred)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author .planning/DECISIONS.md covering every PRD §16 delegated decision</name>
  <files>.planning/DECISIONS.md</files>
  <read_first>
    .planning/phases/01-foundations-decisions-gate/01-RESEARCH.md (every section §A..§G, the Assumptions Log, the Open Questions, the Standard Stack table — DECISIONS.md content draws from each)
    .planning/intel/decisions.md (DEC-001..016 — these are LOCKED PRD-level choices; replicate as `locked` rows for traceability)
    .planning/intel/constraints.md (informational — non-goals do not need rows)
    vibetime-prd.md §16 (the canonical list of delegated decisions)
    .planning/phases/01-foundations-decisions-gate/01-01-SUMMARY.md (confirms what scaffolding actually shipped, in case any decision needs to reference the as-built state)
  </read_first>
  <action>
Create `.planning/DECISIONS.md` with the following exact structure. Every section is mandatory.

**Header (lines 1-10):**
```markdown
# DECISIONS — vibetime V0

> PRD §16 process gate. This file captures every implementation decision delegated by `vibetime-prd.md` §16 to GSD. Decisions in `status: proposed` are awaiting user approval. Until the user replies `approved` (or overrides specific rows), no `core` library code, hook code, or desktop code may be written. PRD §5 LOCKED decisions are replicated below as `status: locked` for traceability — they are NOT up for debate.

**Authored:** 2026-04-28
**Status:** proposed (awaiting user approval per PRD §16)
**Approver:** Barry <barry@yct.ee>
```

**Section 1 — Phase 1 active decisions (proposed):** A markdown table with one row per ID. Use the exact `Choice` and `Rationale` text below; the `Rationale` column must be ONE sentence each (RESEARCH.md mandate).

| ID | Decision | Choice | Rationale (1 sentence) | Decided in Phase | Status |
|----|----------|--------|------------------------|------------------|--------|
| D-MONO | Monorepo tool | pnpm workspaces 10.33.2 | Most mature isolated-store manager; only one battle-tested across React/Next/Electron/Bun-binary mixed stacks (RESEARCH.md §A). | 1 | proposed |
| D-TS | TypeScript strategy | tsconfig.base + project references + per-package `tsconfig.json`; `core` adds `"types": []` | Project references give incremental builds and boundary enforcement; `types: []` mechanically forces `core` runtime-agnosticism (RESEARCH.md §B). | 1 | proposed |
| D-TS-STRICTNESS | TS strict-mode flags | strict + noUncheckedIndexedAccess + exactOptionalPropertyTypes + verbatimModuleSyntax | Catches the exact "never throws" / empty-array bug class the adapter contract forbids (RESEARCH.md §B + A8). | 1 | proposed |
| D-LINT | Lint + format | Biome 2.4.13 (single tool, JSON config) | Replaces ESLint+Prettier; ~50× faster; monorepo-native since v2 (RESEARCH.md §C). | 1 | proposed |
| D-TEST | Test runner for `core` (and Phase 2 adapters) | Vitest 4.1.5 (Node environment) | Node-runnable proves `core` portability across Bun and Electron-main; `bun test` for `hook` is a Phase 3 question (RESEARCH.md §D + A2). | 1 | proposed |
| D-DEPCHECK | Zero-dep enforcement for `core` | Three layers: TS `types: []` + dependency-cruiser 17.3.10 + a 5-line script asserting `packages/core/package.json#dependencies === {}` | A single mechanism is fragile; three cheap layers cover all three failure modes (build-time / lint-time / commit-time) (RESEARCH.md §E). | 1 | proposed |
| D-LAYOUT | Per-package directory layout | `packages/{core,hook,desktop}/src/...` with per-package `tsconfig.json` + `vitest.config.ts`; `core/src/{index,events,schema,project,project.test,schema.test}.ts` | Standard pnpm workspace layout; minimal nesting; each package is independently buildable (RESEARCH.md §"Recommended Project Structure"). | 1 | proposed |
| D-GITPARSE | Git URL parsing in `core` | Hand-rolled regex (≤30 LOC) covering SSH / HTTPS / `ssh://` / `git://`, with credential-stripping; reference: GitHub Desktop `remote-parsing.ts` | Importing `git-url-parse` violates the zero-runtime-dep rule; the parsing logic is mechanically simple and the fixture matrix proves correctness (RESEARCH.md §F). | 1 | proposed |
| D-GITLAB-SUBGROUP | GitLab subgroup project name | `group/sub/repo` (most informative — preserve full path) | A5: balance between informativeness and predictability; the alternative `sub/repo` loses provenance information (RESEARCH.md §F risk note). | 1 | proposed |
| D-CI | CI in Phase 1 | Recommended SKIP — local `pnpm ci` only; defer GitHub Actions to a follow-up | A7: keeps Phase 1 scaffolding minimal; cheap to add `.github/workflows/ci.yml` later if velocity demands; nothing in PRD requires it (RESEARCH.md A7). | 1 | proposed |
| D-PRECOMMIT | Pre-commit hooks (lefthook / simple-git-hooks) | Recommended SKIP for Phase 1; revisit if `pnpm ci` velocity becomes painful | Open Question 3: nothing in PRD requires it; minimal scaffolding wins (RESEARCH.md Open Questions §3). | 1 | proposed |

**Section 2 — Decisions deferred to later phases:**

| ID | Decision | Choice | Rationale (1 sentence) | Decided in Phase | Status |
|----|----------|--------|------------------------|------------------|--------|
| D-IPC | Electron IPC method signatures and types | TBD by Phase 4 | Wait until Today/Live/History view requirements drive the surface; deciding now risks over-design (RESEARCH.md §G). | 4 | deferred |
| D-ANIM | Live view animation library | TBD by Phase 5 | Pick after a Live view design prototype proves the 60fps / ≥30fps acceptance bar; candidates include framer-motion / GSAP / hand-rolled requestAnimationFrame (RESEARCH.md §G). | 5 | deferred |
| D-PKG | Packaging tool (electron-builder vs electron-forge) | TBD by Phase 6 | Choose after a Phase 5 packaging dry-run reveals which one handles `vibetime-hook` bundling + ad-hoc signing more cleanly (RESEARCH.md §G). | 6 | deferred |
| D-SIGN | Ad-hoc signing scripting | TBD by Phase 6 | Locked: ad-hoc only (NOT Apple notarization, per user decision). Scripting choice — `codesign` shell script vs electron-builder built-in vs a Node script — deferred until packaging tool is chosen (RESEARCH.md §G). | 6 | deferred |
| D-TEST-HOOK | Test runner for `hook` package | TBD by Phase 3 | `bun test` is the natural choice for `bun:sqlite`-touching code, but cross-runtime portability questions need a Phase 3 decision (RESEARCH.md A2). | 3 | deferred |

**Section 3 — Locked decisions (replicated for traceability — NOT up for debate):**

A markdown table replicating DEC-001..015 plus the user-locked overrides (MIT license, ad-hoc signing replacing DEC-016 notarization, auto-launch default OFF). Each row's `Status` MUST be `locked`. The `Decided in Phase` column shows the phase that EXECUTES the decision (e.g., DEC-001 Electron → Phase 4).

Include at minimum:
| ID | Decision | Choice | Status | Source |
|----|----------|--------|--------|--------|
| DEC-001 | Application shell | Electron | locked | PRD §5 |
| DEC-002 | Frontend stack | React 18 + TS + Tailwind v4 + coss ui + ECharts v6 + Jotai | locked | PRD §5 |
| DEC-003 | Hook runtime | Bun-compiled standalone CLI binary | locked | PRD §5 |
| DEC-004 | Distribution model | Two binaries: `vibetime-hook` + `vibetime.app` | locked | PRD §5 |
| DEC-005 | Storage | SQLite via `bun:sqlite` (hook) + `better-sqlite3` (Electron main); WAL + standard PRAGMAs | locked | PRD §5 |
| DEC-006 | Repository structure | Monorepo `core` / `hook` / `desktop`; `core` zero-runtime-dep | locked | PRD §5 |
| DEC-007 | Renderer data access | IPC-only; renderer never opens SQLite directly | locked | PRD §5 |
| DEC-008 | Time budget | ~5 weeks; flag any plan exceeding 6 weeks | locked | PRD §5 |
| DEC-009 | Schema | `events` + `open_turns` per PRD §6 verbatim, `schema_version=1`, no migrations | locked | PRD §6 |
| DEC-010 | Project resolution | First-match: alias → git remote → cwd basename → `_unknown` | locked | PRD §6 |
| DEC-011 | Adapter mappings | Per-agent event-name + field-name table; `(payload, event) => NormalizedEvent | null` | locked | PRD §8 |
| DEC-012 | Crash recovery | Orphan sweep on session_start; 6h stale sweep at every desktop launch / CLI invocation | locked | PRD §9 |
| DEC-013 | Menubar metric | Today's cumulative agent time across all projects | locked | PRD §10.1 |
| DEC-014 | Window lifecycle | Close ≠ quit; Cmd+Q or menubar context menu only | locked | PRD §10.4 |
| DEC-015 | Design system | Tokyo Night palette, Inter, JetBrains Mono, ECharts custom theme | locked | PRD §10.5 |
| LIC | License | MIT | locked | User decision |
| SIGN | Code-signing | Ad-hoc self-signed (replaces PRD §14 notarization); first-launch right-click → Open documented | locked | User decision |
| AUTO | Auto-launch on login | Default OFF; opt-in prompt on first launch | locked | User decision |

**Section 4 — Assumptions surfaced for explicit user approval:**

A bulleted list lifted from RESEARCH.md "Assumptions Log" A1..A8, each phrased as a yes/no question:
- A1: Biome (single tool) is acceptable now; an ESLint complement may land in Phase 4 if React rule coverage demands. **Approve?**
- A2: Vitest 4 for `core`; `bun test` decision deferred to Phase 3. **Approve?**
- A3: dependency-cruiser preferred over Biome's `noRestrictedImports`. **Approve?**
- A4: Hand-rolled regex covers ≥99% of real git URLs the user encounters; fixture matrix is the contract. **Approve?**
- A5: GitLab subgroups produce `group/sub/repo` (full path), not `sub/repo`. **Approve?**
- A6: `path.basename` replaced by `cwd.split('/').filter(Boolean).pop()`; POSIX-only is acceptable since V0 is macOS-only. **Approve?**
- A7: No CI in Phase 1 (local `pnpm ci` only). **Approve?**
- A8: `verbatimModuleSyntax: true` is acceptable (forces `import type` everywhere). **Approve?**

**Section 5 — Gate mechanics (verbatim from RESEARCH.md §G):**

```markdown
## Gate mechanics

PRD §16 forbids any non-scaffolding code being written before this file is approved. Phase 1 Plan 01 has shipped the scaffolding. The next plan (`01-03-PLAN.md`) ships the `core` library code (NormalizedEvent, AdapterFn, DDL constants, resolveProject + tests). Plan 03 must NOT execute until the user has reviewed this document and either:

1. Replied **`approved`** (accepts every `proposed` row as-is), OR
2. Replied with **specific overrides** (e.g., "approved EXCEPT D-CI — add the workflow now") which Phase 1 will amend before Plan 03 executes.

Until one of those replies arrives, the workflow STOPS.
```

**Final line of file:** A checklist for the user (markdown):
```markdown
## User review checklist

- [ ] Every `proposed` row above has a clear Choice and one-sentence Rationale
- [ ] Every `deferred` row names a specific later phase
- [ ] No PRD §16 item is missing
- [ ] Open assumptions A1..A8 are surfaced
- [ ] No code has been written outside `package.json` / `tsconfig.json` / `biome.json` / `.dependency-cruiser.cjs` / `LICENSE` / `README.md` / `pnpm-workspace.yaml` / `vitest.config.ts` / `.npmrc` / `packages/*/src/index.ts` placeholders

Reply `approved` to proceed, or list overrides.
```

Do NOT mark any row `approved` in this commit — that status is set retroactively by the human in a follow-up commit after they reply.

The whole file should be ~120-180 lines. If it grows past 250 lines, you have included implementation detail that belongs in a SUMMARY or a follow-up plan.
  </action>
  <verify>
    <automated>test -f .planning/DECISIONS.md &amp;&amp; grep -q "D-MONO" .planning/DECISIONS.md &amp;&amp; grep -q "D-TS\b" .planning/DECISIONS.md &amp;&amp; grep -q "D-TS-STRICTNESS" .planning/DECISIONS.md &amp;&amp; grep -q "D-LINT" .planning/DECISIONS.md &amp;&amp; grep -q "D-TEST\b" .planning/DECISIONS.md &amp;&amp; grep -q "D-DEPCHECK" .planning/DECISIONS.md &amp;&amp; grep -q "D-LAYOUT" .planning/DECISIONS.md &amp;&amp; grep -q "D-GITPARSE" .planning/DECISIONS.md &amp;&amp; grep -q "D-GITLAB-SUBGROUP" .planning/DECISIONS.md &amp;&amp; grep -q "D-CI" .planning/DECISIONS.md &amp;&amp; grep -q "D-PRECOMMIT" .planning/DECISIONS.md &amp;&amp; grep -q "D-IPC" .planning/DECISIONS.md &amp;&amp; grep -q "D-ANIM" .planning/DECISIONS.md &amp;&amp; grep -q "D-PKG" .planning/DECISIONS.md &amp;&amp; grep -q "D-SIGN" .planning/DECISIONS.md &amp;&amp; grep -q "D-TEST-HOOK" .planning/DECISIONS.md &amp;&amp; grep -q "TBD by Phase 3" .planning/DECISIONS.md &amp;&amp; grep -q "TBD by Phase 4" .planning/DECISIONS.md &amp;&amp; grep -q "TBD by Phase 5" .planning/DECISIONS.md &amp;&amp; grep -q "TBD by Phase 6" .planning/DECISIONS.md &amp;&amp; grep -q "Reply .approved." .planning/DECISIONS.md &amp;&amp; [ "$(wc -l &lt; .planning/DECISIONS.md)" -ge 80 ]</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/DECISIONS.md` exists at the repo root.
    - All sixteen required IDs appear at least once: D-MONO, D-TS, D-TS-STRICTNESS, D-LINT, D-TEST, D-DEPCHECK, D-LAYOUT, D-GITPARSE, D-GITLAB-SUBGROUP, D-CI, D-PRECOMMIT, D-IPC, D-ANIM, D-PKG, D-SIGN, D-TEST-HOOK. The verify grep chain mechanically checks every one of these sixteen IDs.
    - All locked DEC-001..015 IDs appear, plus LIC, SIGN, AUTO.
    - The phrase `Reply ` followed by ``approved`` (in markdown code formatting) appears at least once (gate trigger).
    - Each `deferred` row names a specific phase number (3, 4, 5, or 6).
    - File length ≥ 80 lines (proves substantive content) and ≤ 250 lines (proves discipline).
    - No row currently has `Status: approved` (that status is set retroactively by the human reviewer).
  </acceptance_criteria>
  <done>DECISIONS.md is committed and presents every PRD §16 delegated decision with full rationale and explicit deferral phase numbers. The user can review it as a single artifact and reply `approved` or override specific rows.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 — STOP / AWAIT USER APPROVAL — PRD §16 process gate</name>
  <what-built>
    `.planning/DECISIONS.md` covering every PRD §16 delegated decision: monorepo tool (pnpm), TS strategy (project refs + types:[] for core), lint/format (Biome), test runner (Vitest), zero-dep enforcement (3-layer), per-package directory layout, git URL parsing approach (hand-rolled regex with fixture matrix), GitLab subgroup policy, CI strategy, plus explicit deferral of IPC method signatures (Phase 4), animation library (Phase 5), packaging tool + ad-hoc signing scripting (Phase 6), and hook test runner (Phase 3). Eight assumptions A1..A8 are surfaced as yes/no questions. PRD §5 locked decisions and user overrides (MIT, ad-hoc signing, auto-launch OFF) are replicated for traceability.
  </what-built>
  <how-to-verify>
    1. Open `/Users/barry/Documents/Project/i/vibetime/.planning/DECISIONS.md` in your editor.
    2. Confirm every Phase-1 `proposed` row makes sense — Choice column reflects what you want, Rationale is one sentence, you understand the tradeoff.
    3. Confirm every `deferred` row names the right later phase.
    4. Answer the eight A1..A8 assumption questions in your head.
    5. Skim the locked-decisions section for any PRD §5 item that you intended to revisit but didn't.
    6. If everything is fine, reply **`approved`** and Plan 03 (core library code) will proceed.
    7. If you want to override specific rows, reply with the override list, e.g.:
       - `approved EXCEPT D-CI — add a minimal GitHub Actions workflow now`
       - `approved EXCEPT D-GITLAB-SUBGROUP — use sub/repo (drop the group prefix)`
       - `approved EXCEPT A8 — set verbatimModuleSyntax false`
    8. If you want to defer further (e.g., revisit after seeing a real fixture), say `defer D-XXX to Phase Y`.
  </how-to-verify>
  <resume-signal>
    Reply `approved` to accept all proposed rows and proceed to Plan 03.
    Reply with overrides (e.g., `approved EXCEPT D-XXX — <new choice>`) to amend specific rows before Plan 03 executes.
    Reply `revise` to request a redraft of any section.
  </resume-signal>
  <acceptance_criteria>
    - Workflow halts and does NOT proceed to Plan 03 until the user supplies an explicit signal (`approved` / overrides / `revise`).
    - On `approved`, the executor commits the user's reply to git history (e.g., a follow-up commit message `chore: PRD §16 gate cleared — DECISIONS.md approved by Barry`) and updates each Phase-1 `proposed` row to `approved`.
    - On override, the executor amends the named rows, commits the diff, and re-asks for confirmation before continuing.
  </acceptance_criteria>
  <done>User has supplied an approval signal AND, if approvals included overrides, those overrides are applied to DECISIONS.md and committed before Plan 03 starts. PROC-01 is satisfied only after this checkpoint clears.</done>
</task>

</tasks>

<verification>
- `.planning/DECISIONS.md` exists, is committed, and matches the row schema.
- Every PRD §16 delegated decision is present (proposed or deferred); none are silently absent.
- The plan terminated with a manual checkpoint; the executor did NOT auto-proceed to Plan 03.
- After user approval, the `Status` column for Phase-1 rows transitions from `proposed` to `approved` in a follow-up commit.
</verification>

<success_criteria>
PROC-01 satisfied: DECISIONS.md exists, captures every PRD §16 delegated decision, and the user has explicitly approved (or overridden specific rows) before any non-scaffolding code is written. No `core` / `hook` / `desktop` source file beyond the `export {}` placeholders from Plan 01 has been touched.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-decisions-gate/01-02-SUMMARY.md` documenting:
- The exact user reply that cleared the gate (verbatim, with timestamp).
- Any overrides applied, with diff references.
- Confirmation that no code beyond Plan 01's scaffolding placeholders has been touched.
- Explicit handoff: "Plan 03 (core library) is now unblocked."
</output>
</output>
