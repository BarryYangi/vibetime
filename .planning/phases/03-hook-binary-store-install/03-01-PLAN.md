---
phase: 03-hook-binary-store-install
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/hook/src/constants.ts
  - packages/hook/src/fs.ts
  - packages/hook/src/config.ts
  - packages/hook/src/log.ts
  - packages/hook/src/index.ts
autonomous: true
requirements: [FS-01, FS-02, FS-03, HOOK-03]
user_setup: []

must_haves:
  truths:
    - "~/.vibetime/ 目录以 0700 权限创建"
    - "config.toml 包含 [projects] 和 [display].timezone 默认值"
    - "hook.log 在首次写入时懒创建"
    - "日志轮转在 10MB 时触发"
  artifacts:
    - path: "packages/hook/src/constants.ts"
      provides: "VERSION, DB_PATH, LOG_PATH, CONFIG_PATH, MAX_LOG_SIZE, STALE_TURN_MAX_AGE 常量"
      exports: ["VERSION", "DB_PATH", "LOG_PATH", "CONFIG_PATH", "MAX_LOG_SIZE", "STALE_TURN_MAX_AGE"]
    - path: "packages/hook/src/fs.ts"
      provides: "ensureVibetimeDir() 函数"
      exports: ["ensureVibetimeDir"]
    - path: "packages/hook/src/config.ts"
      provides: "readConfig(), writeConfig() 函数"
      exports: ["readConfig", "writeConfig", "VibetimeConfig"]
    - path: "packages/hook/src/log.ts"
      provides: "appendLog() 函数，带日志轮转"
      exports: ["appendLog"]
    - path: "packages/hook/src/index.ts"
      provides: "argv[0] 自路由入口点"
      exports: []
  key_links:
    - from: "packages/hook/src/fs.ts"
      to: "~/.vibetime/"
      via: "mkdirSync with mode 0o700"
      pattern: "mkdirSync.*0o700"
    - from: "packages/hook/src/config.ts"
      to: "~/.vibetime/config.toml"
      via: "readFileSync / writeFileSync"
      pattern: "config\\.toml"
    - from: "packages/hook/src/log.ts"
      to: "~/.vibetime/hook.log"
      via: "appendFileSync with rotation"
      pattern: "hook\\.log"
---

<objective>
实现 Phase 3 基础设施层：常量定义、目录初始化、配置读写、日志轮转。

Purpose: 为后续 Store、Hook、CLI 模块提供基础设施，确保 ~/.vibetime/ 目录结构正确创建，配置文件可读写，日志可记录。

Output: constants.ts, fs.ts, config.ts, log.ts 四个模块，通过 bun test 验证。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-hook-binary-store-install/03-CONTEXT.md
@.planning/phases/03-hook-binary-store-install/03-RESEARCH.md
@.planning/phases/03-hook-binary-store-install/03-PATTERNS.md
@packages/hook/package.json
@packages/hook/tsconfig.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建常量定义和文件系统初始化模块</name>
  <files>packages/hook/src/constants.ts, packages/hook/src/fs.ts</files>
  <read_first>
    - packages/hook/package.json（确认包结构）
    - packages/hook/tsconfig.json（确认 TypeScript 配置）
    - .planning/phases/03-hook-binary-store-install/03-RESEARCH.md（参考 Pattern 1 和 Pattern 3）
    - .planning/phases/03-hook-binary-store-install/03-PATTERNS.md（参考 constants.ts 和 fs.ts 模式）
  </read_first>
  <action>
**constants.ts:**
```typescript
export const VERSION = /* BUILD_VERSION via --define */ '0.0.0-dev'
export const DB_PATH = `${process.env.HOME}/.vibetime/data.db`
export const LOG_PATH = `${process.env.HOME}/.vibetime/hook.log`
export const CONFIG_PATH = `${process.env.HOME}/.vibetime/config.toml`
export const MAX_LOG_SIZE = 10 * 1024 * 1024 // 10MB
export const STALE_TURN_MAX_AGE = 6 * 60 * 60 // 6 hours (per REC-02)
```

**fs.ts:**
```typescript
import { mkdirSync } from 'node:fs'
import { CONFIG_PATH, DB_PATH, LOG_PATH } from './constants.js'

/**
 * Ensure ~/.vibetime/ directory exists with mode 0700.
 * Creates config.toml with defaults if it doesn't exist.
 * Returns the directory path.
 */
export function ensureVibetimeDir(): string {
  const dir = `${process.env.HOME}/.vibetime`
  mkdirSync(dir, { recursive: true, mode: 0o700 })
  return dir
}
```

**关键约束：**
- 目录权限必须是 0o700（FS-01）
- 使用 node:fs 的 mkdirSync（RESEARCH.md Don't Hand-Roll）
- constants.ts 使用 `as const` 确保类型安全
  </action>
  <verify>
    <automated>cd /Users/barry/Documents/Project/i/vibetime && bun test packages/hook/src/constants.test.ts packages/hook/src/fs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export const VERSION" packages/hook/src/constants.ts
    - grep -q "export const DB_PATH" packages/hook/src/constants.ts
    - grep -q "export const LOG_PATH" packages/hook/src/constants.ts
    - grep -q "export const CONFIG_PATH" packages/hook/src/constants.ts
    - grep -q "export const MAX_LOG_SIZE" packages/hook/src/constants.ts
    - grep -q "export const STALE_TURN_MAX_AGE" packages/hook/src/constants.ts
    - grep -q "0o700" packages/hook/src/fs.ts
    - grep -q "export function ensureVibetimeDir" packages/hook/src/fs.ts
    - bun test packages/hook/src/constants.test.ts passes
    - bun test packages/hook/src/fs.test.ts passes
  </acceptance_criteria>
  <done>
    - constants.ts 导出所有必需常量
    - fs.ts 导出 ensureVibetimeDir() 函数
    - 目录以 0700 权限创建
    - 测试通过
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建配置读写和日志轮转模块</name>
  <files>packages/hook/src/config.ts, packages/hook/src/log.ts</files>
  <read_first>
    - packages/hook/src/constants.ts（确认常量定义）
    - packages/hook/src/fs.ts（确认目录初始化逻辑）
    - .planning/phases/03-hook-binary-store-install/03-RESEARCH.md（参考 Pattern 3 Log Rotation 和 Bun TOML loader）
    - .planning/phases/03-hook-binary-store-install/03-PATTERNS.md（参考 config.ts 和 log.ts 模式）
    - .planning/phases/03-hook-binary-store-install/03-CONTEXT.md（确认 config.toml 结构）
  </read_first>
  <action>
**config.ts:**
```typescript
import { readFileSync, writeFileSync, existsSync } from 'node:fs'
import { CONFIG_PATH } from './constants.js'
import { ensureVibetimeDir } from './fs.js'

export interface VibetimeConfig {
  projects: Record<string, string>
  display: {
    timezone: string
  }
}

const DEFAULT_CONFIG: VibetimeConfig = {
  projects: {},
  display: {
    timezone: Intl.DateTimeFormat().resolvedOptions().timeZone
  }
}

/**
 * Read config.toml, creating it with defaults if it doesn't exist.
 * Uses Bun's native TOML loader if available, otherwise falls back to simple parsing.
 */
export function readConfig(): VibetimeConfig {
  ensureVibetimeDir()

  if (!existsSync(CONFIG_PATH)) {
    writeConfig(DEFAULT_CONFIG)
    return DEFAULT_CONFIG
  }

  try {
    const raw = readFileSync(CONFIG_PATH, 'utf-8')
    // Simple TOML parsing for V0 (only need [projects] and [display])
    const config = parseToml(raw)
    return {
      projects: config.projects ?? {},
      display: {
        timezone: config.display?.timezone ?? DEFAULT_CONFIG.display.timezone
      }
    }
  } catch {
    return DEFAULT_CONFIG
  }
}

/**
 * Write config to config.toml.
 */
export function writeConfig(config: VibetimeConfig): void {
  ensureVibetimeDir()
  const content = serializeToml(config)
  writeFileSync(CONFIG_PATH, content, 'utf-8')
}

/**
 * Simple TOML serializer for V0 config structure.
 * Only handles flat keys and [section] tables.
 */
function serializeToml(config: VibetimeConfig): string {
  const lines: string[] = []
  lines.push('[projects]')
  for (const [key, value] of Object.entries(config.projects)) {
    lines.push(`${key} = "${value}"`)
  }
  lines.push('')
  lines.push('[display]')
  lines.push(`timezone = "${config.display.timezone}"`)
  return lines.join('\n') + '\n'
}

/**
 * Simple TOML parser for V0 config structure.
 * Only handles flat keys and [section] tables.
 */
function parseToml(raw: string): Record<string, unknown> {
  const result: Record<string, unknown> = {}
  let currentSection: Record<string, string> = {}
  let currentSectionName = ''

  for (const line of raw.split('\n')) {
    const trimmed = line.trim()
    if (!trimmed || trimmed.startsWith('#')) continue

    const sectionMatch = trimmed.match(/^\[(.+)\]$/)
    if (sectionMatch) {
      if (currentSectionName) {
        result[currentSectionName] = currentSection
      }
      currentSectionName = sectionMatch[1]
      currentSection = {}
      continue
    }

    const kvMatch = trimmed.match(/^(\w+)\s*=\s*"?(.+?)"?$/)
    if (kvMatch && currentSectionName) {
      currentSection[kvMatch[1]] = kvMatch[2].replace(/^"|"$/g, '')
    }
  }

  if (currentSectionName) {
    result[currentSectionName] = currentSection
  }

  return result
}
```

**log.ts:**
```typescript
import { statSync, renameSync, appendFileSync, mkdirSync } from 'node:fs'
import { LOG_PATH, MAX_LOG_SIZE } from './constants.js'
import { ensureVibetimeDir } from './fs.js'

/**
 * Append a message to hook.log with rotation at ~10MB.
 * Never throws — hook must never surface errors.
 */
export function appendLog(message: string): void {
  try {
    ensureVibetimeDir()

    // Check for rotation
    try {
      const stat = statSync(LOG_PATH)
      if (stat.size > MAX_LOG_SIZE) {
        renameSync(LOG_PATH, `${LOG_PATH}.1`)
      }
    } catch {
      // File doesn't exist yet — fine
    }

    const line = `[${new Date().toISOString()}] ${message}\n`
    appendFileSync(LOG_PATH, line)
  } catch {
    // Last resort: swallow. Hook must never surface errors.
  }
}
```

**关键约束：**
- 日志轮转在 10MB 时触发（HOOK-03）
- appendLog 永远不抛出异常（HOOK-02 exit 0 约束）
- config.toml 使用简单 TOML 解析（Bun 原生 TOML 是 import 语法，运行时不可用）
- 配置读写需要先确保目录存在（FS-01）
  </action>
  <verify>
    <automated>cd /Users/barry/Documents/Project/i/vibetime && bun test packages/hook/src/config.test.ts packages/hook/src/log.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export interface VibetimeConfig" packages/hook/src/config.ts
    - grep -q "export function readConfig" packages/hook/src/config.ts
    - grep -q "export function writeConfig" packages/hook/src/config.ts
    - grep -q "export function appendLog" packages/hook/src/log.ts
    - grep -q "MAX_LOG_SIZE" packages/hook/src/log.ts
    - grep -q "renameSync.*hook\\.log\\.1" packages/hook/src/log.ts
    - bun test packages/hook/src/config.test.ts passes
    - bun test packages/hook/src/log.test.ts passes
  </acceptance_criteria>
  <done>
    - config.ts 导出 VibetimeConfig 接口和 readConfig/writeConfig 函数
    - log.ts 导出 appendLog 函数，带日志轮转
    - 配置读写创建默认 config.toml（FS-02）
    - 日志轮转在 10MB 时触发（HOOK-03）
    - 测试通过
  </done>
</task>

<task type="auto">
  <name>Task 3: 更新 package.json 和入口点路由</name>
  <files>packages/hook/package.json, packages/hook/src/index.ts</files>
  <read_first>
    - packages/hook/package.json（当前配置）
    - packages/hook/src/index.ts（当前入口点）
    - .planning/phases/03-hook-binary-store-install/03-CONTEXT.md（确认 argv[0] 自路由决策）
    - .planning/phases/03-hook-binary-store-install/03-PATTERNS.md（参考 index.ts 模式）
  </read_first>
  <action>
**package.json 更新：**
```json
{
  "name": "@vibetime/hook",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "test": "bun test",
    "build": "bun build --compile --minify --bytecode src/index.ts --outfile vibetime-hook"
  },
  "dependencies": {
    "@vibetime/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "@types/node": "^22"
  }
}
```

**index.ts 更新：**
```typescript
import { basename } from 'node:path'

/**
 * Entry point: argv[0] basename routing.
 * - vibetime-hook → HOOK mode (stdin → adapter → SQLite)
 * - vibetime → CLI mode (parse subcommands)
 * - vibetime (no args) → CLI mode special (launch Electron app)
 */
async function main(): Promise<void> {
  const self = basename(process.argv[1] ?? '')

  if (self === 'vibetime-hook') {
    // HOOK mode: stdin → adapter → SQLite
    await import('./hook.js')
  } else {
    // CLI mode: parse subcommands
    await import('./cli.js')
  }
}

main()
```

**关键约束：**
- 测试脚本改为 `bun test`（CONTEXT.md 灰色区域 2）
- 移除 vitest devDependency（bun test 内置）
- 添加 build 脚本用于 Bun 编译（HOOK-01）
- argv[0] 自路由（CONTEXT.md 灰色区域 1）
  </action>
  <verify>
    <automated>cd /Users/barry/Documents/Project/i/vibetime && cat packages/hook/package.json | grep -q '"test": "bun test"' && cat packages/hook/src/index.ts | grep -q "vibetime-hook"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q '"test": "bun test"' packages/hook/package.json
    - grep -q '"build": "bun build' packages/hook/package.json
    - grep -q "basename(process.argv\[1\])" packages/hook/src/index.ts
    - grep -q "vibetime-hook" packages/hook/src/index.ts
    - grep -q "await import('./hook.js')" packages/hook/src/index.ts
    - grep -q "await import('./cli.js')" packages/hook/src/index.ts
    - packages/hook/package.json 不包含 vitest devDependency
  </acceptance_criteria>
  <done>
    - package.json 测试脚本改为 bun test
    - package.json 添加 build 脚本
    - index.ts 实现 argv[0] 自路由
    - vitest 依赖移除
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stdin → hook | 外部 agent 发送的 JSON payload 可能是恶意的 |
| config.toml → hook | 用户配置文件可能被篡改 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Denial of Service | stdin payload | mitigate | JSON.parse in try/catch; exit 0 on failure |
| T-03-02 | Tampering | config.toml | accept | 本地文件，无网络暴露；用户自行保护 |
| T-03-03 | Tampering | hook.log | accept | 日志文件无敏感信息，仅供调试 |
</threat_model>

<verification>
Phase 3 Plan 01 完成后验证：
1. `bun test packages/hook` 所有测试通过
2. `ls -la ~/.vibetime/` 目录存在且权限为 0700
3. `cat ~/.vibetime/config.toml` 包含 [projects] 和 [display] 默认值
4. `grep -q "export const VERSION" packages/hook/src/constants.ts` 存在
5. `grep -q "export function ensureVibetimeDir" packages/hook/src/fs.ts` 存在
6. `grep -q "export function readConfig" packages/hook/src/config.ts` 存在
7. `grep -q "export function appendLog" packages/hook/src/log.ts` 存在
</verification>

<success_criteria>
- constants.ts 导出所有 6 个常量
- fs.ts 导出 ensureVibetimeDir() 函数
- config.ts 导出 VibetimeConfig 接口和 readConfig/writeConfig 函数
- log.ts 导出 appendLog() 函数
- index.ts 实现 argv[0] 自路由
- package.json 测试脚本改为 bun test
- 所有测试通过（bun test packages/hook）
</success_criteria>

<output>
After completion, create `.planning/phases/03-hook-binary-store-install/03-01-SUMMARY.md`
</output>
