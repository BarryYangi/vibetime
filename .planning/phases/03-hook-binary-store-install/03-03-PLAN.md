---
phase: 03-hook-binary-store-install
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - packages/hook/src/hook.ts
  - packages/hook/src/recovery.ts
autonomous: true
requirements: [REC-01, REC-02, HOOK-01, HOOK-02, HOOK-04]
user_setup: []

must_haves:
  truths:
    - "session_start 时查询 open_turns 并创建 synthetic turn_end 事件"
    - "stale open_turns（>6 小时）在 CLI 调用时被清理"
    - "Hook 读取 stdin payload 并调用匹配的 adapter"
    - "Hook 永远 exit 0，即使解析失败"
    - "Hook 无 stdout/stderr 输出"
  artifacts:
    - path: "packages/hook/src/hook.ts"
      provides: "Hook 模式主逻辑：stdin → adapter → SQLite"
      exports: ["runHook"]
    - path: "packages/hook/src/recovery.ts"
      provides: "recoverOrphans(), sweepStale() 函数"
      exports: ["recoverOrphans", "sweepStale"]
  key_links:
    - from: "packages/hook/src/hook.ts"
      to: "@vibetime/core"
      via: "import adapters"
      pattern: "import.*adaptClaudeCode.*adaptCodex.*adaptCursor.*from.*@vibetime/core"
    - from: "packages/hook/src/hook.ts"
      to: "packages/hook/src/store.ts"
      via: "openDatabase, persistEvent"
      pattern: "import.*openDatabase.*persistEvent.*from.*\\.\\/store"
    - from: "packages/hook/src/recovery.ts"
      to: "packages/hook/src/store.ts"
      via: "queryOpenTurns, deleteOpenTurn"
      pattern: "import.*queryOpenTurns.*deleteOpenTurn.*from.*\\.\\/store"
---

<objective>
实现 Phase 3 Hook 二进制主逻辑和崩溃恢复：stdin 读取、adapter 分发、SQLite 持久化、孤儿清理、stale 清理。

Purpose: 关闭数据捕获循环，确保 agent 会话事件正确写入数据库，崩溃后能恢复。

Output: hook.ts 和 recovery.ts 模块，通过 bun test 验证。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-hook-binary-store-install/03-CONTEXT.md
@.planning/phases/03-hook-binary-store-install/03-RESEARCH.md
@.planning/phases/03-hook-binary-store-install/03-PATTERNS.md
@packages/core/src/index.ts
@packages/hook/src/store.ts
@packages/hook/src/constants.ts
@packages/hook/src/config.ts
@packages/hook/src/log.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现崩溃恢复模块</name>
  <files>packages/hook/src/recovery.ts</files>
  <read_first>
    - packages/hook/src/store.ts（确认 queryOpenTurns, deleteOpenTurn 函数）
    - .planning/phases/03-hook-binary-store-install/03-RESEARCH.md（参考 Pattern 2 和 Open Questions §3）
    - .planning/phases/03-hook-binary-store-install/03-PATTERNS.md（参考 recovery.ts 模式）
    - .planning/phases/03-hook-binary-store-install/03-CONTEXT.md（确认 REC-01, REC-02 需求）
  </read_first>
  <action>
**recovery.ts:**
```typescript
import type { Database } from 'bun:sqlite'
import type { NormalizedEvent } from '@vibetime/core'
import { queryOpenTurns, deleteOpenTurn, persistEvent } from './store.js'
import { appendLog } from './log.js'
import { STALE_TURN_MAX_AGE } from './constants.js'

/**
 * Recover orphaned open_turns for a specific session.
 * Called on session_start to handle crashes.
 * Creates synthetic turn_end events with meta.abandoned = true.
 */
export function recoverOrphans(db: Database, sessionId: string): void {
  try {
    const orphans = queryOpenTurns(db, sessionId)

    for (const orphan of orphans) {
      // Create synthetic turn_end event
      const syntheticEvent: NormalizedEvent = {
        agent: orphan.agent as NormalizedEvent['agent'],
        event_type: 'turn_end',
        project: orphan.project,
        session_id: orphan.session_id,
        turn_id: orphan.turn_id,
        ts: Math.floor(Date.now() / 1000),
        timezone: orphan.timezone,
        duration_sec: null, // Unknown duration for abandoned turns
        meta: { abandoned: true }
      }

      // Persist the synthetic event
      persistEvent(db, syntheticEvent)

      // Delete the orphan (already done by persistEvent for turn_end, but explicit for safety)
      deleteOpenTurn(db, orphan.turn_id)

      appendLog(`Recovered orphan turn ${orphan.turn_id} for session ${sessionId}`)
    }
  } catch (err) {
    // Recovery failure should not block hook main flow
    appendLog(`Error in recoverOrphans: ${err}`)
  }
}

/**
 * Sweep stale open_turns older than STALE_TURN_MAX_AGE.
 * Called on CLI/desktop launch, NOT on hook hot path.
 * Creates synthetic turn_end events with meta.reason = "stale_sweep".
 */
export function sweepStale(db: Database): void {
  try {
    const cutoff = Math.floor(Date.now() / 1000) - STALE_TURN_MAX_AGE
    const allOpenTurns = queryOpenTurns(db)

    for (const turn of allOpenTurns) {
      if (turn.started_at < cutoff) {
        // Create synthetic turn_end event
        const syntheticEvent: NormalizedEvent = {
          agent: turn.agent as NormalizedEvent['agent'],
          event_type: 'turn_end',
          project: turn.project,
          session_id: turn.session_id,
          turn_id: turn.turn_id,
          ts: Math.floor(Date.now() / 1000),
          timezone: turn.timezone,
          duration_sec: null, // Unknown duration for stale turns
          meta: { reason: 'stale_sweep' }
        }

        // Persist the synthetic event
        persistEvent(db, syntheticEvent)

        // Delete the stale turn (already done by persistEvent for turn_end, but explicit for safety)
        deleteOpenTurn(db, turn.turn_id)

        appendLog(`Swept stale turn ${turn.turn_id} (started at ${turn.started_at})`)
      }
    }
  } catch (err) {
    // Sweep failure should not block CLI/desktop
    appendLog(`Error in sweepStale: ${err}`)
  }
}
```

**关键约束：**
- recoverOrphans 在 session_start 时调用（REC-01）
- sweepStale 在 CLI/desktop 启动时调用，不在 hook 热路径（REC-02）
- 创建 synthetic turn_end 事件，duration_sec = NULL（REC-01, REC-02）
- 使用 meta.abandoned = true 标记孤儿（REC-01）
- 使用 meta.reason = "stale_sweep" 标记 stale（REC-02）
- 恢复失败不应阻断主流程（never-throws contract）
  </action>
  <verify>
    <automated>cd /Users/barry/Documents/Project/i/vibetime && bun test packages/hook/src/recovery.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export function recoverOrphans" packages/hook/src/recovery.ts
    - grep -q "export function sweepStale" packages/hook/src/recovery.ts
    - grep -q "meta.*abandoned.*true" packages/hook/src/recovery.ts
    - grep -q "meta.*reason.*stale_sweep" packages/hook/src/recovery.ts
    - grep -q "duration_sec.*null" packages/hook/src/recovery.ts
    - grep -q "STALE_TURN_MAX_AGE" packages/hook/src/recovery.ts
    - bun test packages/hook/src/recovery.test.ts passes
  </acceptance_criteria>
  <done>
    - recovery.ts 导出 recoverOrphans 和 sweepStale 函数
    - recoverOrphans 创建 synthetic turn_end 事件（meta.abandoned = true）
    - sweepStale 创建 synthetic turn_end 事件（meta.reason = "stale_sweep"）
    - duration_sec = NULL（未知持续时间）
    - 恢复失败不阻断主流程
    - 测试通过
  </done>
</task>

<task type="auto">
  <name>Task 2: 实现 Hook 模式主逻辑</name>
  <files>packages/hook/src/hook.ts</files>
  <read_first>
    - packages/hook/src/store.ts（确认 openDatabase, persistEvent, closeDatabase 函数）
    - packages/hook/src/recovery.ts（确认 recoverOrphans 函数）
    - packages/hook/src/config.ts（确认 readConfig 函数）
    - packages/hook/src/log.ts（确认 appendLog 函数）
    - packages/core/src/index.ts（确认 adapter 导出）
    - .planning/phases/03-hook-binary-store-install/03-RESEARCH.md（参考 Complete Hook Entry Point）
    - .planning/phases/03-hook-binary-store-install/03-PATTERNS.md（参考 hook.ts 模式）
    - .planning/phases/03-hook-binary-store-install/03-CONTEXT.md（确认 agent 检测决策）
  </read_first>
  <action>
**hook.ts:**
```typescript
import { adaptClaudeCode, adaptCodex, adaptCursor, resolveProject } from '@vibetime/core'
import type { Agent, NormalizedEvent } from '@vibetime/core'
import { openDatabase, persistEvent, closeDatabase } from './store.js'
import { recoverOrphans } from './recovery.js'
import { readConfig } from './config.js'
import { appendLog } from './log.js'

/**
 * Detect agent type from event name or --source argument.
 * Primary: event name matching against known agent event types.
 * Fallback: --source argument (Claude Code supports this).
 */
function detectAgent(payload: Record<string, unknown>, argv: string[]): Agent | null {
  // Check --source argument first
  const sourceIdx = argv.indexOf('--source')
  if (sourceIdx !== -1 && argv[sourceIdx + 1]) {
    const source = argv[sourceIdx + 1].toLowerCase()
    if (source === 'claude' || source === 'claude-code') return 'claude-code'
    if (source === 'codex') return 'codex'
    if (source === 'cursor') return 'cursor'
  }

  // Event name matching
  const eventName = (payload.hook_event_name || payload.event || '') as string

  // Claude Code events
  if (['UserPromptSubmit', 'Stop', 'SessionStart', 'SessionEnd'].includes(eventName)) {
    return 'claude-code'
  }

  // Codex events (same as Claude Code for now)
  if (['UserPromptSubmit', 'Stop', 'SessionStart'].includes(eventName)) {
    return 'codex'
  }

  // Cursor events
  if (['beforeSubmitPrompt', 'stop', 'sessionStart', 'sessionEnd'].includes(eventName)) {
    return 'cursor'
  }

  return null
}

/**
 * Get git remote URL for project resolution.
 * Uses Bun.spawnSync for fast execution.
 */
function getGitRemoteUrl(cwd: string): string | null {
  try {
    const result = Bun.spawnSync(['git', '-C', cwd, 'config', '--get', 'remote.origin.url'], {
      stdout: 'pipe',
      stderr: 'pipe',
    })
    if (result.exitCode === 0 && result.stdout) {
      return result.stdout.toString().trim()
    }
    return null
  } catch {
    return null
  }
}

/**
 * Run the hook in HOOK mode.
 * Reads stdin, parses JSON, calls adapter, resolves project, persists to SQLite.
 * Always exits 0, even on error.
 */
export async function runHook(): Promise<void> {
  let db: ReturnType<typeof openDatabase> | null = null

  try {
    // 1. Read stdin
    const raw = await Bun.stdin.text()
    if (!raw.trim()) {
      process.exit(0)
    }

    // 2. Parse JSON
    let payload: Record<string, unknown>
    try {
      payload = JSON.parse(raw)
    } catch (err) {
      appendLog(`Failed to parse stdin JSON: ${err}`)
      process.exit(0)
    }

    // 3. Detect agent
    const agent = detectAgent(payload, process.argv)
    if (!agent) {
      appendLog(`Could not detect agent from event: ${JSON.stringify(payload)}`)
      process.exit(0)
    }

    // 4. Call adapter
    const adapters = {
      'claude-code': adaptClaudeCode,
      'codex': adaptCodex,
      'cursor': adaptCursor,
    }

    const adapter = adapters[agent]
    const eventName = (payload.hook_event_name || payload.event || '') as string
    const event = adapter(payload, eventName)

    if (!event) {
      // Adapter returned null — no-op
      process.exit(0)
    }

    // 5. Resolve project
    const config = readConfig()
    const gitRemoteUrl = getGitRemoteUrl(event.project) // event.project is raw cwd
    event.project = resolveProject({
      cwd: event.project,
      aliases: config.projects,
      gitRemoteUrl: gitRemoteUrl ?? undefined,
    })

    // 6. Persist to SQLite
    db = openDatabase()
    persistEvent(db, event)

    // 7. Crash recovery on session_start
    if (event.event_type === 'session_start') {
      recoverOrphans(db, event.session_id)
    }
  } catch (err) {
    // Last line of defense — never throw
    appendLog(`Unhandled error in hook: ${err}`)
  } finally {
    // Close database connection
    if (db) {
      closeDatabase(db)
    }
    // Always exit 0 (PRD §7)
    process.exit(0)
  }
}
```

**关键约束：**
- Hook 永远 exit 0（HOOK-02, PRD §7）
- 无 stdout/stderr 输出（HOOK-02）
- 错误写入 hook.log（HOOK-03）
- 使用 core adapters（HOOK-04）
- 使用 resolveProject 处理项目名称（Phase 2 决策）
- session_start 时调用 recoverOrphans（REC-01）
- Agent 检测：先检查 --source 参数，再匹配事件名（CONTEXT.md 灰色区域 3）
- getGitRemoteUrl 使用 Bun.spawnSync（RESEARCH.md Don't Hand-Roll）
  </action>
  <verify>
    <automated>cd /Users/barry/Documents/Project/i/vibetime && bun test packages/hook/src/hook.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export async function runHook" packages/hook/src/hook.ts
    - grep -q "function detectAgent" packages/hook/src/hook.ts
    - grep -q "function getGitRemoteUrl" packages/hook/src/hook.ts
    - grep -q "adaptClaudeCode.*adaptCodex.*adaptCursor" packages/hook/src/hook.ts
    - grep -q "resolveProject" packages/hook/src/hook.ts
    - grep -q "recoverOrphans" packages/hook/src/hook.ts
    - grep -q "process.exit(0)" packages/hook/src/hook.ts
    - grep -q "appendLog" packages/hook/src/hook.ts
    - bun test packages/hook/src/hook.test.ts passes
  </acceptance_criteria>
  <done>
    - hook.ts 导出 runHook 函数
    - detectAgent 实现 agent 检测（--source 参数 + 事件名匹配）
    - getGitRemoteUrl 使用 Bun.spawnSync
    - 使用 core adapters 处理事件
    - 使用 resolveProject 解析项目名称
    - session_start 时调用 recoverOrphans
    - 永远 exit 0
    - 测试通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stdin → hook | 外部 agent 发送的 JSON payload 可能是恶意的 |
| hook → SQLite | 事件数据写入数据库，需防止注入 |
| hook → git | 执行 git 命令获取 remote URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Denial of Service | stdin payload | mitigate | JSON.parse in try/catch; exit 0 on failure |
| T-03-08 | Tampering | git command injection | mitigate | 使用数组参数，不拼接字符串 |
| T-03-09 | Elevation of Privilege | process.exit(0) | accept | Hook 设计要求永远 exit 0，即使出错 |
</threat_model>

<verification>
Phase 3 Plan 03 完成后验证：
1. `bun test packages/hook/src/recovery.test.ts` 所有测试通过
2. `bun test packages/hook/src/hook.test.ts` 所有测试通过
3. `grep -q "export function recoverOrphans" packages/hook/src/recovery.ts` 存在
4. `grep -q "export function sweepStale" packages/hook/src/recovery.ts` 存在
5. `grep -q "export async function runHook" packages/hook/src/hook.ts` 存在
6. `grep -q "process.exit(0)" packages/hook/src/hook.ts` 存在
7. `grep -q "recoverOrphans" packages/hook/src/hook.ts` 存在
</verification>

<success_criteria>
- recovery.ts 导出 recoverOrphans 和 sweepStale 函数
- hook.ts 导出 runHook 函数
- Agent 检测实现（--source 参数 + 事件名匹配）
- 使用 core adapters 处理事件（HOOK-04）
- 使用 resolveProject 解析项目名称
- Crash recovery 实现（REC-01, REC-02）
- Hook 永远 exit 0（HOOK-02）
- 所有测试通过（bun test packages/hook/src/recovery.test.ts, bun test packages/hook/src/hook.test.ts）
</success_criteria>

<output>
After completion, create `.planning/phases/03-hook-binary-store-install/03-03-SUMMARY.md`
</output>
