---
phase: 05-live-history-menubar-lifecycle
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - packages/desktop/package.json
  - packages/desktop/src/renderer/src/App.tsx
  - packages/desktop/src/renderer/src/components/Sidebar.tsx
  - packages/desktop/src/renderer/src/views/Live.tsx
  - packages/desktop/src/renderer/src/views/History.tsx
  - packages/desktop/src/renderer/src/charts/theme.ts
autonomous: true
requirements: [LIVE-01, LIVE-02, LIVE-03, LIVE-04, HIST-01, HIST-02, HIST-03, HIST-04, HIST-05]
must_haves:
  truths:
    - "Sidebar includes Today, Live, History, Settings"
    - "Live route renders active-turn stage and idle ambient state"
    - "Live uses motion/react and not framer-motion"
    - "Concurrent active turns stack vertically and remain visible"
    - "History renders ECharts calendar heatmap, stacked area, and sortable coss Table"
    - "History period selector supports 7d, 30d, 90d, 365d"
  artifacts:
    - path: "packages/desktop/src/renderer/src/views/Live.tsx"
      provides: "Live view"
    - path: "packages/desktop/src/renderer/src/views/History.tsx"
      provides: "History view"
---

<objective>
Build the renderer-facing Live and History views using the data contracts from Plan 05-01 and the visual contract from UI-SPEC.
</objective>

<context>
@.planning/phases/05-live-history-menubar-lifecycle/05-CONTEXT.md
@.planning/phases/05-live-history-menubar-lifecycle/05-UI-SPEC.md
@.planning/phases/05-live-history-menubar-lifecycle/05-RESEARCH.md
@.planning/phases/05-live-history-menubar-lifecycle/05-PATTERNS.md
@packages/desktop/src/renderer/src/App.tsx
@packages/desktop/src/renderer/src/components/Sidebar.tsx
@packages/desktop/src/renderer/src/views/Today.tsx
@packages/desktop/src/renderer/src/charts/theme.ts
@packages/desktop/src/renderer/src/components/ui/table.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Motion dependency and routes/navigation</name>
  <files>
    packages/desktop/package.json,
    packages/desktop/src/renderer/src/App.tsx,
    packages/desktop/src/renderer/src/components/Sidebar.tsx
  </files>
  <read_first>
    packages/desktop/package.json,
    packages/desktop/src/renderer/src/App.tsx,
    packages/desktop/src/renderer/src/components/Sidebar.tsx,
    .planning/phases/05-live-history-menubar-lifecycle/05-UI-SPEC.md
  </read_first>
  <action>
Add dependency `"motion"` to `packages/desktop/package.json`. Add routes:

- `/live` -> `Live`
- `/history` -> `History`

Add sidebar items:

- Today: existing `/`
- Live: `/live`, use a lucide icon such as `RadioIcon` or `ActivityIcon`
- History: `/history`, use `CalendarDaysIcon` or `HistoryIcon`
- Settings: existing `/settings`

Do not remove the existing Today default route.
  </action>
  <verify>
    <automated>rtk pnpm install --lockfile-only && rtk pnpm --filter @vibetime/desktop build</automated>
  </verify>
  <acceptance_criteria>
    - `packages/desktop/package.json` contains `"motion"`
    - `packages/desktop/src/renderer/src/App.tsx` contains `path="/live"`
    - `packages/desktop/src/renderer/src/App.tsx` contains `path="/history"`
    - `packages/desktop/src/renderer/src/components/Sidebar.tsx` contains `Live`
    - `packages/desktop/src/renderer/src/components/Sidebar.tsx` contains `History`
    - `packages/desktop/src/renderer/src/App.tsx` still contains `path="/"`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement Live view stage</name>
  <files>
    packages/desktop/src/renderer/src/views/Live.tsx
  </files>
  <read_first>
    packages/desktop/src/renderer/src/views/Today.tsx,
    packages/desktop/src/renderer/src/store.ts,
    packages/desktop/src/shared/ipc-types.ts,
    .planning/phases/05-live-history-menubar-lifecycle/05-UI-SPEC.md
  </read_first>
  <action>
Create `Live.tsx`.

Required behavior:

- import from `motion/react`
- use `getTodayLiveState` / `todayLiveStateAtom` or equivalent store helper from Plan 05-01
- show active turns with:
  - project name large
  - agent/model line where model appears only if present
  - per-second elapsed timer in mono/tabular numerals
  - breathing pulse at about 1Hz
  - moving sweep bar
  - today's project total footer
- stack concurrent turns vertically and keep all visible
- idle state copy:
  - heading `No active turn`
  - body `Start a coding-agent turn and it will appear here.`
  - subtle ambient motion
- do not render Today project-list layout in idle state
  </action>
  <verify>
    <automated>rtk pnpm --filter @vibetime/desktop build</automated>
  </verify>
  <acceptance_criteria>
    - `packages/desktop/src/renderer/src/views/Live.tsx` imports from `motion/react`
    - `packages/desktop/src/renderer/src/views/Live.tsx` contains `No active turn`
    - `packages/desktop/src/renderer/src/views/Live.tsx` contains `Start a coding-agent turn`
    - `packages/desktop/src/renderer/src/views/Live.tsx` maps active turns
    - `packages/desktop/src/renderer/src/views/Live.tsx` contains a pulse animation using Motion or CSS
    - `packages/desktop/src/renderer/src/views/Live.tsx` contains a sweep bar element
    - `rtk pnpm --filter @vibetime/desktop build` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Implement History analytics view</name>
  <files>
    packages/desktop/src/renderer/src/views/History.tsx
  </files>
  <read_first>
    packages/desktop/src/renderer/src/charts/theme.ts,
    packages/desktop/src/renderer/src/components/ui/table.tsx,
    packages/desktop/src/renderer/src/store.ts,
    packages/desktop/src/shared/ipc-types.ts,
    .planning/phases/05-live-history-menubar-lifecycle/05-UI-SPEC.md
  </read_first>
  <action>
Create `History.tsx`.

Required behavior:

- period selector with exact labels `7d`, `30d`, `90d`, `365d`
- fetch `getHistorySummary({ periodDays })`
- render ECharts native calendar heatmap for 365 days using calendar coordinate system
- render ECharts stacked area trend chart with Top 5 + Others series
- render sortable coss `Table` with columns Project, Total, Turns, Last Active
- use `cossNeutral` theme
- empty state copy:
  - heading `No history yet`
  - body `Captured turns will appear here after your first completed session.`
  </action>
  <verify>
    <automated>rtk pnpm --filter @vibetime/desktop build</automated>
  </verify>
  <acceptance_criteria>
    - `packages/desktop/src/renderer/src/views/History.tsx` contains `7d`
    - `packages/desktop/src/renderer/src/views/History.tsx` contains `30d`
    - `packages/desktop/src/renderer/src/views/History.tsx` contains `90d`
    - `packages/desktop/src/renderer/src/views/History.tsx` contains `365d`
    - `packages/desktop/src/renderer/src/views/History.tsx` contains `calendar`
    - `packages/desktop/src/renderer/src/views/History.tsx` contains `heatmap`
    - `packages/desktop/src/renderer/src/views/History.tsx` contains `areaStyle`
    - `packages/desktop/src/renderer/src/views/History.tsx` imports coss Table components
    - `packages/desktop/src/renderer/src/views/History.tsx` contains `Project`
    - `packages/desktop/src/renderer/src/views/History.tsx` contains `Last Active`
    - `rtk pnpm --filter @vibetime/desktop build` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local metrics -> visual display | Project names and timing metrics become visible on streamable screens |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Information Disclosure | Live/History views | accept | Local desktop tool; user controls stream visibility |
</threat_model>

<verification>
- `rtk pnpm --filter @vibetime/desktop build`
- Manual visual pass at 960x640 for Live idle, one active turn, multiple active turns
- Manual visual pass for History with no data and with synthetic data
</verification>

<success_criteria>
- Live route is distinct from Today and focused on active turns.
- History route renders all required chart/table surfaces.
- Build passes with Motion and ECharts usage.
</success_criteria>
