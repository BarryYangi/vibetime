---
phase: 05-live-history-menubar-lifecycle
plan: 04
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - packages/desktop/src/main/index.ts
  - packages/desktop/src/main/ipc-handlers.ts
  - packages/desktop/src/renderer/src/views/Settings.tsx
  - packages/desktop/src/renderer/src/App.tsx
  - .planning/phases/05-live-history-menubar-lifecycle/05-VERIFICATION.md
autonomous: true
requirements: [SET-04, LIFE-01, LIFE-02, LIVE-04, HIST-05, MENU-01, MENU-02, MENU-03, MENU-04, MENU-05, MENU-06]
must_haves:
  truths:
    - "Settings exposes Open at login toggle"
    - "Auto-launch state uses Electron login item settings and config"
    - "Auto-launch prompt appears after user has context, not on first app launch"
    - "Last active route is persisted and restored"
    - "Phase 5 manual verification evidence is recorded"
  artifacts:
    - path: "packages/desktop/src/renderer/src/views/Settings.tsx"
      provides: "Open at login toggle and prompt"
    - path: ".planning/phases/05-live-history-menubar-lifecycle/05-VERIFICATION.md"
      provides: "Manual verification checklist/evidence"
---

<objective>
Finish Phase 5 by integrating auto-launch settings, last-view restore, delayed prompt behavior, and a manual verification evidence file for native and visual acceptance criteria.
</objective>

<context>
@.planning/phases/05-live-history-menubar-lifecycle/05-CONTEXT.md
@.planning/phases/05-live-history-menubar-lifecycle/05-UI-SPEC.md
@.planning/phases/05-live-history-menubar-lifecycle/05-RESEARCH.md
@.planning/phases/05-live-history-menubar-lifecycle/05-VALIDATION.md
@packages/desktop/src/renderer/src/views/Settings.tsx
@packages/desktop/src/main/index.ts
@packages/desktop/src/main/ipc-handlers.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync app preferences with Electron login item APIs</name>
  <files>
    packages/desktop/src/main/ipc-handlers.ts,
    packages/desktop/src/main/index.ts
  </files>
  <read_first>
    packages/desktop/src/main/ipc-handlers.ts,
    packages/desktop/src/main/index.ts,
    packages/hook/src/config.ts,
    .planning/phases/05-live-history-menubar-lifecycle/05-RESEARCH.md
  </read_first>
  <action>
Update `getAppPreferences` and `updateAppPreferences` so `openAtLogin` syncs with Electron:

- `getAppPreferences` reads config and `app.getLoginItemSettings()`.
- `updateAppPreferences({ openAtLogin })` calls `app.setLoginItemSettings({ openAtLogin })`.
- Config still records `app.open_at_login`.
- Return normalized `AppPreferences`.

Do not call login item APIs from renderer.
  </action>
  <verify>
    <automated>rtk pnpm --filter @vibetime/desktop build</automated>
  </verify>
  <acceptance_criteria>
    - `packages/desktop/src/main/ipc-handlers.ts` contains `setLoginItemSettings`
    - `packages/desktop/src/main/ipc-handlers.ts` contains `getLoginItemSettings`
    - `packages/desktop/src/main/ipc-handlers.ts` contains `openAtLogin`
    - `rtk pnpm --filter @vibetime/desktop build` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add Settings Open at login toggle and delayed opt-in prompt</name>
  <files>
    packages/desktop/src/renderer/src/views/Settings.tsx
  </files>
  <read_first>
    packages/desktop/src/renderer/src/views/Settings.tsx,
    packages/desktop/src/shared/ipc-types.ts,
    packages/desktop/src/renderer/src/store.ts,
    .planning/phases/05-live-history-menubar-lifecycle/05-UI-SPEC.md
  </read_first>
  <action>
Add an `Open at login` toggle to Settings.

Prompt behavior:

- Do not prompt immediately on first app launch.
- Prompt after first successful Settings open or first successful agent connection when `autoLaunchPrompted === false`.
- Prompt copy:
  - heading `Open VibeTime at login?`
  - body `Keep the menubar timer available after you sign in.`
  - accept `Open at login`
  - decline `Not now`
- Accept sets `openAtLogin: true` and `autoLaunchPrompted: true`.
- Decline sets `autoLaunchPrompted: true`.
  </action>
  <verify>
    <automated>rtk pnpm --filter @vibetime/desktop build</automated>
  </verify>
  <acceptance_criteria>
    - `packages/desktop/src/renderer/src/views/Settings.tsx` contains `Open at login`
    - `packages/desktop/src/renderer/src/views/Settings.tsx` contains `Open VibeTime at login?`
    - `packages/desktop/src/renderer/src/views/Settings.tsx` contains `Keep the menubar timer available after you sign in.`
    - `packages/desktop/src/renderer/src/views/Settings.tsx` contains `Not now`
    - `packages/desktop/src/renderer/src/views/Settings.tsx` invokes `updateAppPreferences`
    - `rtk pnpm --filter @vibetime/desktop build` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Persist and restore last active route</name>
  <files>
    packages/desktop/src/renderer/src/App.tsx,
    packages/desktop/src/main/index.ts
  </files>
  <read_first>
    packages/desktop/src/renderer/src/App.tsx,
    packages/desktop/src/main/index.ts,
    packages/desktop/src/shared/ipc-types.ts
  </read_first>
  <action>
Persist last active route through app preferences:

- First-run default remains `/`.
- Renderer updates `lastView` when route changes among `/`, `/live`, `/history`, `/settings`.
- Menubar/Open behavior restores `lastView`.
- `/menubar` must not become `lastView`.
  </action>
  <verify>
    <automated>rtk pnpm --filter @vibetime/desktop build</automated>
  </verify>
  <acceptance_criteria>
    - `packages/desktop/src/renderer/src/App.tsx` contains `lastView`
    - `packages/desktop/src/renderer/src/App.tsx` excludes `/menubar` from persisted routes
    - `packages/desktop/src/main/index.ts` restores a route when opening the main window
    - `rtk pnpm --filter @vibetime/desktop build` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Record Phase 5 verification evidence</name>
  <files>
    .planning/phases/05-live-history-menubar-lifecycle/05-VERIFICATION.md
  </files>
  <read_first>
    .planning/phases/05-live-history-menubar-lifecycle/05-VALIDATION.md,
    .planning/REQUIREMENTS.md
  </read_first>
  <action>
Create `.planning/phases/05-live-history-menubar-lifecycle/05-VERIFICATION.md` with checklist sections:

- Live view: active state, multiple turns, idle state, smooth animation evidence
- History view: 365-day heatmap, stacked area, sortable table, period selector
- Menubar: title states, left click, right click, refresh cadence
- Lifecycle: close hides, Cmd+Q quits, Tray Quit quits
- Auto-launch: Settings toggle, prompt timing, login item state
- Commands run: include exact output summary for `rtk pnpm --filter @vibetime/desktop test`, `rtk pnpm --filter @vibetime/desktop build`, and `rtk pnpm run ci`

Use status `partial` if manual checks cannot be completed in the current environment; use status `passed` only when manual evidence is recorded.
  </action>
  <verify>
    <manual>Open the verification file and ensure every Phase 5 requirement ID is represented.</manual>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/05-live-history-menubar-lifecycle/05-VERIFICATION.md` contains `LIVE-01`
    - `.planning/phases/05-live-history-menubar-lifecycle/05-VERIFICATION.md` contains `HIST-01`
    - `.planning/phases/05-live-history-menubar-lifecycle/05-VERIFICATION.md` contains `MENU-01`
    - `.planning/phases/05-live-history-menubar-lifecycle/05-VERIFICATION.md` contains `SET-04`
    - `.planning/phases/05-live-history-menubar-lifecycle/05-VERIFICATION.md` contains `LIFE-01`
    - `.planning/phases/05-live-history-menubar-lifecycle/05-VERIFICATION.md` contains `rtk pnpm run ci`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer -> login item IPC | Renderer asks main to mutate OS login item behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Elevation of Privilege | login item setting | mitigate | Only expose boolean openAtLogin preference; no arbitrary launch path input from renderer |
| T-05-07 | Repudiation | auto-launch prompt | mitigate | Persist `auto_launch_prompted` so prompt is not repeatedly shown |
</threat_model>

<verification>
- `rtk pnpm --filter @vibetime/desktop test`
- `rtk pnpm --filter @vibetime/desktop build`
- `rtk pnpm run ci`
- Manual native checks recorded in `05-VERIFICATION.md`
</verification>

<success_criteria>
- Open at login is visible and backed by Electron login item state.
- Prompt timing follows context decision.
- Last active view restore works without persisting `/menubar`.
- Phase 5 has recorded verification evidence.
</success_criteria>
