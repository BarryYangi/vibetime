---
phase: 07-usage-analytics
plan: 03
type: execute
wave: 3
depends_on: [07-02]
files_modified:
  - packages/core/src/usage/pricing.ts
  - packages/core/src/usage/pricing.test.ts
  - packages/core/src/usage/reconcile.ts
  - packages/core/src/usage/reconcile.test.ts
  - packages/core/src/usage/aggregate.ts
  - packages/core/src/usage/aggregate.test.ts
  - packages/core/src/usage/types.ts
  - packages/core/src/index.ts
  - .planning/phases/07-usage-analytics/07-USAGE-MAPPING.md
autonomous: true
requirements: [TOK-01, TOK-02, USAGE-01, USAGE-05, USAGE-06, USAGE-07]
must_haves:
  truths:
    - "Unknown model pricing yields visible tokens with nullable estimated cost, never `$0.00`."
    - "Usage rows link to hook turns by turn id first, session/time window second, project/time window third, and unmatched rows remain visible."
    - "Aggregates include Claude Code and Codex only and produce project, model, agent, daily, audit, and unassigned summaries."
    - "The phase contains `07-USAGE-MAPPING.md` before renderer chart implementation begins."
    - "D-14: `07-USAGE-MAPPING.md` explicitly analyzes project spend, model used per turn, model efficiency, cache hit rate, cost/time relationship, time spent per model, daily/weekly/monthly cost-token-time trends, and aggregate token/cost analysis by project/model/agent."
    - "D-15: The mapping separates transcript-only insights from hook-linked insights because hook data supplies project/session/turn/time context."
    - "D-16: Final MVP chart choices are selected only after scanner fields are proven by tests and the mapping artifact."
  artifacts:
    - path: "packages/core/src/usage/pricing.ts"
      provides: "Pricing normalization and estimated-cost calculator"
    - path: "packages/core/src/usage/reconcile.ts"
      provides: "Usage-to-hook attribution"
    - path: "packages/core/src/usage/aggregate.ts"
      provides: "Usage summary builder for IPC/UI"
    - path: ".planning/phases/07-usage-analytics/07-USAGE-MAPPING.md"
      provides: "Data capability -> hook linkage -> user value -> visualization map"
  key_links:
    - from: "packages/core/src/usage/reconcile.ts"
      to: "packages/core/src/usage/aggregate.ts"
      via: "attributed UsageRecordFact inputs"
      pattern: "attribution_method"
    - from: ".planning/phases/07-usage-analytics/07-USAGE-MAPPING.md"
      to: "packages/desktop/src/renderer/src/views/Usage.tsx"
      via: "later UI plan must choose charts from supported mapping rows"
      pattern: "Daily usage trend|Token breakdown|Project spend ranking|Audit table"
---

<objective>
Implement estimated-cost calculation, hook-linked attribution, Usage aggregation, and the required mapping artifact.

Purpose: Convert scanner facts into honest product analytics before any final chart choices are locked.
Output: Tested pricing, reconciliation, aggregation modules, plus `07-USAGE-MAPPING.md`.
</objective>

<execution_context>
@/Users/barry/.codex/get-shit-done/workflows/execute-plan.md
@/Users/barry/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-usage-analytics/07-CONTEXT.md
@.planning/phases/07-usage-analytics/07-SPEC.md
@.planning/phases/07-usage-analytics/07-RESEARCH.md
@.planning/phases/07-usage-analytics/07-VALIDATION.md
@.planning/phases/07-usage-analytics/07-PATTERNS.md
@.planning/phases/07-usage-analytics/07-UI-SPEC.md
@packages/core/src/history.ts
@packages/core/src/usage/types.ts
@packages/core/src/usage/__fixtures__/pricing-cache.json
@packages/core/src/usage/__fixtures__/hook-events.ts

<interfaces>
Aggregators should mirror `buildHistorySummaryFromEvents(events, { periodDays, now })`:
```typescript
export function buildUsageSummary(records: UsageRecordFact[], options: UsageSummaryArgs): UsageSummary
```
Cost calculation must return `null` for unknown model pricing:
```typescript
export function estimateUsageCostUsd(tokens: UsageTokenBreakdown, price: UsagePricingEntry | null): number | null
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement pricing normalization and nullable cost estimates</name>
  <files>packages/core/src/usage/pricing.ts, packages/core/src/usage/pricing.test.ts, packages/core/src/usage/types.ts, packages/core/src/index.ts</files>
  <read_first>
    - `packages/core/src/usage/pricing.ts`
    - `packages/core/src/usage/pricing.test.ts`
    - `packages/core/src/usage/types.ts`
    - `packages/core/src/usage/__fixtures__/pricing-cache.json`
    - `.planning/phases/07-usage-analytics/07-CONTEXT.md`
    - `.planning/phases/07-usage-analytics/07-RESEARCH.md`
    - `.planning/phases/07-usage-analytics/07-VALIDATION.md`
  </read_first>
  <behavior>
    - Known model cost uses per-million public rates for input, cached input, cache creation, and output tokens.
    - Unknown model cost returns `null`.
    - Malformed pricing payloads are ignored and do not convert unknown cost to zero.
  </behavior>
  <action>Create `pricing.ts` with `normalizeLiteLlmPricingPayload(payload, fetchedAt)`, `estimateUsageCostUsd(tokens, price)`, `lookupUsagePrice(model, prices)`, and `pricingStatusFromCache(cache, now)`. Use the ccusage-aligned LiteLLM pricing shape and map fields to `UsagePricingEntry`: `model`, `provider`, `input_per_million_usd`, `cached_input_per_million_usd`, `cache_creation_input_per_million_usd`, `output_per_million_usd`, `source: 'litellm'`, `fetched_at`, `raw_version`. Treat `reasoning_output_tokens` as part of output-token reporting but do not add it a second time if `output_tokens` already includes reasoning. Add tests for seeded cache success, refresh payload normalization, stale-cache fallback status, unknown model returns `null`, and malformed payload ignored. Export pricing functions from `index.ts`.</action>
  <verify>
    <automated>rtk pnpm --filter @vibetime/core test -- src/usage/pricing.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rtk rg -n "estimateUsageCostUsd|normalizeLiteLlmPricingPayload|lookupUsagePrice|pricingStatusFromCache" packages/core/src/usage/pricing.ts packages/core/src/index.ts` finds exports.
    - `rtk rg -n "unknown model|returns null|malformed|stale" packages/core/src/usage/pricing.test.ts` finds negative cases.
    - Pricing tests pass.
  </acceptance_criteria>
  <done>Cost estimation is derived from token facts plus validated pricing entries and preserves unknown-cost states.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement hook reconciliation and Usage aggregation</name>
  <files>packages/core/src/usage/reconcile.ts, packages/core/src/usage/reconcile.test.ts, packages/core/src/usage/aggregate.ts, packages/core/src/usage/aggregate.test.ts, packages/core/src/usage/types.ts, packages/core/src/index.ts</files>
  <read_first>
    - `packages/core/src/usage/reconcile.ts`
    - `packages/core/src/usage/reconcile.test.ts`
    - `packages/core/src/usage/aggregate.ts`
    - `packages/core/src/usage/aggregate.test.ts`
    - `packages/core/src/usage/types.ts`
    - `packages/core/src/history.ts`
    - `packages/core/src/usage/__fixtures__/hook-events.ts`
    - `.planning/phases/07-usage-analytics/07-SPEC.md`
    - `.planning/phases/07-usage-analytics/07-UI-SPEC.md`
  </read_first>
  <behavior>
    - Attribution tries `turn_id` match, then same `session_id` time window, then same project/time window, then `unmatched`.
    - Missing project linkage does not drop rows; rows remain in agent/model totals and in an `Unassigned usage` audit bucket.
    - Aggregates use only `agent === 'claude-code' || agent === 'codex'`.
    - Daily arrays are dense for the selected 7/30/90/365 period.
  </behavior>
  <action>Create `reconcileUsageWithHookEvents(records, hookEvents)` and `buildUsageSummary(records, options)` in `reconcile.ts` and `aggregate.ts`. Use `UsageAttributionMethod` values exactly: `turn_id`, `session_time_window`, `project_time_window`, `unmatched`. Preserve unmatched rows with `project: null` and `attribution_confidence: 0`. Build `UsageSummary` with `periodDays`, `totals` (`totalTokens`, nullable `estimatedCostUsd`, `unknownCostTokens`, `recordCount`), `daily` dense rows, `byAgent`, `byModel`, `byProject`, `tokenBreakdown`, `auditRows`, `availableFilters`, and `pricingStatus`. Exclude Cursor/Gemini records from all Usage totals. Include cost when `estimateUsageCostUsd` returns a number; keep `estimatedCostUsd: null` when no rows in a group are priceable. Add tests for all attribution methods, unassigned preservation, Claude/Codex-only filtering, dense daily rows, unknown price audit rows, and project/model/agent breakdown sorting.</action>
  <verify>
    <automated>rtk pnpm --filter @vibetime/core test -- src/usage/reconcile.test.ts src/usage/aggregate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rtk rg -n "turn_id|session_time_window|project_time_window|unmatched" packages/core/src/usage/reconcile.ts packages/core/src/usage/reconcile.test.ts` finds all attribution methods.
    - `rtk rg -n "claude-code|codex|Cursor|Gemini|Unassigned usage|unknown" packages/core/src/usage/aggregate.test.ts` finds scope and negative cases.
    - Reconcile and aggregate tests pass.
  </acceptance_criteria>
  <done>Usage facts can be attributed to hook context and summarized without losing unassigned or unknown-price rows.</done>
</task>

<task type="auto">
  <name>Task 3: Write data capability to visualization mapping</name>
  <files>.planning/phases/07-usage-analytics/07-USAGE-MAPPING.md</files>
  <read_first>
    - `.planning/phases/07-usage-analytics/07-USAGE-MAPPING.md`
    - `.planning/phases/07-usage-analytics/07-CONTEXT.md`
    - `.planning/phases/07-usage-analytics/07-SPEC.md`
    - `.planning/phases/07-usage-analytics/07-RESEARCH.md`
    - `.planning/phases/07-usage-analytics/07-UI-SPEC.md`
    - `packages/core/src/usage/codex-scanner.ts`
    - `packages/core/src/usage/claude-scanner.ts`
    - `packages/core/src/usage/aggregate.ts`
    - `packages/core/src/usage/reconcile.ts`
  </read_first>
  <action>Create `.planning/phases/07-usage-analytics/07-USAGE-MAPPING.md` after scanner, pricing, reconciliation, and aggregation tests pass. Include a table with columns `Product Question`, `Data Capability`, `Hook Linkage`, `User Value`, `Visualization`, and `Supported In MVP`. Cover exactly these product questions from D-14: project spend, model used per turn, model efficiency, cache hit rate, cost/time relationship, time spent per model, daily/weekly/monthly trends for cost/tokens/time, and aggregated token/cost analysis by project/model/agent. Mark transcript-only insights and hook-linked insights separately per D-15, and mark unsupported insights distinctly. Choose final MVP visualizations only after proven scanner fields per D-16 and only from `07-UI-SPEC.md`: metric tiles, daily usage trend, token breakdown, project spend ranking, model efficiency table, cost/time relationship unavailable panel if linkage is insufficient, daily trend with compact weekly/monthly stat rows, and audit table. Do not mention Cursor or Gemini as chart options.</action>
  <verify>
    <automated>test -f .planning/phases/07-usage-analytics/07-USAGE-MAPPING.md && rtk rg -n "Project spend|Model used per turn|Model efficiency|Cache hit rate|Cost/time relationship|Time spent per model|daily|weekly|monthly|Metric tiles|Daily usage trend|Token breakdown|Audit table" .planning/phases/07-usage-analytics/07-USAGE-MAPPING.md</automated>
  </verify>
  <acceptance_criteria>
    - `rtk rg -n "transcript-only|hook-linked|unsupported" .planning/phases/07-usage-analytics/07-USAGE-MAPPING.md` finds all three support categories.
    - `rtk rg -n "Cursor|Gemini|cursor|gemini" .planning/phases/07-usage-analytics/07-USAGE-MAPPING.md` returns no chart option rows.
    - Mapping exists before `packages/desktop/src/renderer/src/views/Usage.tsx` is created in Plan 06.
  </acceptance_criteria>
  <done>The required mapping artifact exists and locks supported Usage visualizations from proven data capabilities.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Token facts -> pricing calculator | Public pricing metadata may be missing, stale, or malformed. |
| Usage facts -> hook attribution | Missing session/turn/project linkage must not drop or misattribute rows. |
| Mapping artifact -> UI plan | UI charts must not imply unsupported precision. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-pricing-integrity | Tampering | `pricing.ts` | mitigate | Validate pricing shape, return nullable cost for unknown/malformed prices, never represent unknown as `$0.00`. |
| T-attribution-integrity | Tampering | `reconcile.ts` | mitigate | Ordered attribution methods with confidence; unmatched rows preserved. |
| T-scan-integrity | Tampering | `aggregate.ts` | mitigate | Filter allowed usage agents and test Cursor/Gemini exclusion. |
| T-chart-trust | Repudiation | `07-USAGE-MAPPING.md` | mitigate | Explicit transcript-only/hook-linked/unsupported categories before UI charts. |
| T-usage-privacy | Information Disclosure | aggregate/audit rows | mitigate | Aggregates use normalized facts only; no transcript content accepted as input. |
</threat_model>

<verification>
Run:
- `rtk pnpm --filter @vibetime/core test -- src/usage/pricing.test.ts src/usage/reconcile.test.ts src/usage/aggregate.test.ts`
- `test -f .planning/phases/07-usage-analytics/07-USAGE-MAPPING.md`
- `! rtk rg -n "vibetime usage|usage export|exportUsage" packages` must pass, proving no new CLI/export surface.
</verification>

<success_criteria>
Plan succeeds when pricing, attribution, and aggregation tests pass and the mapping artifact covers all D-14 product questions without adding unsupported charts or deferred agent scope.
</success_criteria>

<output>
After completion, create `.planning/phases/07-usage-analytics/07-03-SUMMARY.md`.
</output>
