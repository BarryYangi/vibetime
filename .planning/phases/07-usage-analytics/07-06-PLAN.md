---
phase: 07-usage-analytics
plan: 06
type: execute
wave: 6
depends_on: [07-03, 07-05]
files_modified:
  - packages/desktop/src/renderer/src/store.ts
  - packages/desktop/src/renderer/src/usage-store.test.ts
  - packages/desktop/src/renderer/src/App.tsx
  - packages/desktop/src/renderer/src/components/Sidebar.tsx
  - packages/desktop/src/renderer/src/views/Usage.tsx
  - packages/desktop/src/renderer/src/views/Settings.tsx
  - packages/desktop/src/renderer/src/locales/en.json
  - packages/desktop/src/renderer/src/locales/zh.json
autonomous: true
requirements: [TOK-02, USAGE-01, USAGE-05, USAGE-06, USAGE-07, USAGE-08]
must_haves:
  truths:
    - "Usage appears as a dedicated route below History and before Settings in the sidebar."
    - "Usage renders Claude Code and Codex token totals, nullable estimated cost, agent/model/project breakdowns, pricing/cache states, and unassigned audit rows."
    - "Usage charts use ECharts and chart choices come from `07-USAGE-MAPPING.md`."
    - "Settings exposes Usage refresh frequency with exact options 15m, 30m, 1h, and 4h."
    - "Cursor/Gemini totals, legends, filters, scanner support, CLI, and export UI are absent."
    - "D-01: Usage is a dedicated page/module placed below History in navigation, not a tab nested inside the History page."
    - "D-02: Usage reuses History visual language, dashboard layout patterns, and period selector conventions without becoming the same page or subordinate History mode."
    - "D-03: Usage has its own `/usage` route and navigation item immediately after History while History remains focused on time retrospectives."
  artifacts:
    - path: "packages/desktop/src/renderer/src/views/Usage.tsx"
      provides: "Dedicated Usage dashboard"
    - path: "packages/desktop/src/renderer/src/store.ts"
      provides: "Usage summary cache, refresh action, and push refresh handling"
    - path: "packages/desktop/src/renderer/src/App.tsx"
      provides: "`/usage` route and last-view persistence"
    - path: "packages/desktop/src/renderer/src/components/Sidebar.tsx"
      provides: "Usage navigation item after History"
    - path: "packages/desktop/src/renderer/src/views/Settings.tsx"
      provides: "Usage refresh frequency Settings row"
  key_links:
    - from: "packages/desktop/src/renderer/src/views/Usage.tsx"
      to: "packages/desktop/src/renderer/src/store.ts"
      via: "refreshUsageSummary and runUsageRefresh"
      pattern: "refreshUsageSummary|runUsageRefresh"
    - from: "Usage route open"
      to: "cache summary read -> background refresh/pricing refresh -> summary re-read"
      via: "`Usage.tsx` derives current args, calls `refreshUsageSummary(args)`, triggers `runUsageRefresh()`/`refreshUsage`, then calls `refreshUsageSummary(args)` again on success"
      pattern: "refreshUsageSummary\\(.*runUsageRefresh|runUsageRefresh\\(.*refreshUsageSummary"
    - from: "packages/desktop/src/renderer/src/store.ts"
      to: "packages/desktop/src/shared/ipc-types.ts"
      via: "typed IPC channels getUsageSummary and refreshUsage"
      pattern: "getUsageSummary|refreshUsage"
    - from: "packages/desktop/src/renderer/src/views/Usage.tsx"
      to: ".planning/phases/07-usage-analytics/07-USAGE-MAPPING.md"
      via: "visualization inventory from mapping artifact"
      pattern: "Daily usage trend|Token breakdown|Audit table"
---

<objective>
Build the dedicated Usage renderer surface and Settings control.

Purpose: Surface Claude/Codex token and estimated-cost analytics through the existing coss/ECharts desktop UI without violating IPC, privacy, or scope boundaries.
Output: Usage route, sidebar item, renderer store cache, ECharts dashboard, Settings refresh-frequency row, and localized strings.
</objective>

<execution_context>
@/Users/barry/.codex/get-shit-done/workflows/execute-plan.md
@/Users/barry/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-usage-analytics/07-CONTEXT.md
@.planning/phases/07-usage-analytics/07-SPEC.md
@.planning/phases/07-usage-analytics/07-RESEARCH.md
@.planning/phases/07-usage-analytics/07-VALIDATION.md
@.planning/phases/07-usage-analytics/07-PATTERNS.md
@.planning/phases/07-usage-analytics/07-UI-SPEC.md
@.planning/phases/07-usage-analytics/07-USAGE-MAPPING.md
@packages/desktop/src/renderer/src/store.ts
@packages/desktop/src/renderer/src/store.test.ts
@packages/desktop/src/renderer/src/App.tsx
@packages/desktop/src/renderer/src/components/Sidebar.tsx
@packages/desktop/src/renderer/src/views/History.tsx
@packages/desktop/src/renderer/src/views/Settings.tsx
@packages/desktop/src/renderer/src/locales/en.json
@packages/desktop/src/renderer/src/locales/zh.json

<interfaces>
Use existing renderer cache/push pattern:
```typescript
export async function refreshHistorySummary(periodDays: HistorySummary['periodDays']): Promise<IpcResult<HistorySummary> | null>
export function handlePush(event: IpcPushEvent): void
```
Use route pattern:
```tsx
const LAST_VIEW_ROUTES = new Set(['/', '/live', '/history', '/settings'])
const Usage = lazy(() => import('./views/Usage'))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Usage renderer store cache and refresh tests</name>
  <files>packages/desktop/src/renderer/src/store.ts, packages/desktop/src/renderer/src/usage-store.test.ts</files>
  <read_first>
    - `packages/desktop/src/renderer/src/store.ts`
    - `packages/desktop/src/renderer/src/store.test.ts`
    - `packages/desktop/src/renderer/src/usage-store.test.ts`
    - `packages/desktop/src/shared/ipc-types.ts`
    - `.planning/phases/07-usage-analytics/07-UI-SPEC.md`
    - `.planning/phases/07-usage-analytics/07-VALIDATION.md`
  </read_first>
  <behavior>
    - Store caches Usage summaries by period and filter key.
    - Stale async Usage refreshes are ignored when a newer refresh wins.
    - `usage-changed` push refreshes Usage only after Usage has loaded once.
    - Refresh failure preserves stale visible data and returns error state.
    - Page-open/store flow renders cached summary first, triggers one Usage/pricing refresh, re-reads the summary on success, and preserves cached summary on refresh failure.
  </behavior>
  <action>Extend `store.ts` with `usageSummariesAtom`, `usageRefreshStateAtom`, `activeUsageQuery`, `refreshUsageSummary(args)`, `runUsageRefresh()`, and `clearActiveUsageQuery()`. Use a sequence map like `historyRefreshSeqByPeriod`, keyed by serialized `UsageSummaryArgs`. Define the Usage page-open flow contract for `Usage.tsx`: derive the current period/filter `UsageSummaryArgs`; call `refreshUsageSummary(args)` first so cached DB summary renders immediately; trigger `runUsageRefresh()` exactly once on mount/open so the main process performs usage scan plus pricing refresh through typed `refreshUsage`; after success call `refreshUsageSummary(args)` again so historical estimates and charts reflect refreshed pricing/transcript data; after failure keep the cached summary visible, and when no usable price/cache exists keep cost fields `Unknown` while token totals remain visible. In `handlePush`, refresh Usage on `usage-changed` and `db-changed` only when `activeUsageQuery` is not null and at least one usage summary is cached. Create `usage-store.test.ts` with tests for cache-first summary storage, stale refresh protection, page-open/store flow triggering refresh while preserving cached summary on refresh failure, manual refresh invoking `refreshUsage`, and push refresh gated by prior Usage load.</action>
  <verify>
    <automated>rtk pnpm --filter @vibetime/desktop test -- src/renderer/src/usage-store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rtk rg -n "usageSummariesAtom|refreshUsageSummary|runUsageRefresh|usage-changed|activeUsageQuery" packages/desktop/src/renderer/src/store.ts packages/desktop/src/renderer/src/usage-store.test.ts` finds store and tests.
    - `packages/desktop/src/renderer/src/usage-store.test.ts` or equivalent covers Usage open flow: first `refreshUsageSummary(args)` renders cached data, one `runUsageRefresh()`/`refreshUsage` is triggered, success re-queries summary, and refresh failure preserves the cached summary with visible token totals.
    - Usage store test exits 0.
    - `rtk rg -n "sqlite|better-sqlite3|readFile|fetch\\(" packages/desktop/src/renderer/src/store.ts packages/desktop/src/renderer/src/usage-store.test.ts` returns no direct DB/filesystem/network access.
  </acceptance_criteria>
  <done>Renderer has a typed Usage cache and refresh flow with stale-result protection and no direct SQLite/filesystem/network work.</done>
</task>

<task type="auto">
  <name>Task 2: Add dedicated Usage route, sidebar item, and ECharts dashboard</name>
  <files>packages/desktop/src/renderer/src/App.tsx, packages/desktop/src/renderer/src/components/Sidebar.tsx, packages/desktop/src/renderer/src/views/Usage.tsx</files>
  <read_first>
    - `packages/desktop/src/renderer/src/App.tsx`
    - `packages/desktop/src/renderer/src/components/Sidebar.tsx`
    - `packages/desktop/src/renderer/src/views/Usage.tsx`
    - `packages/desktop/src/renderer/src/views/History.tsx`
    - `packages/desktop/src/renderer/src/charts/theme.ts`
    - `.planning/phases/07-usage-analytics/07-UI-SPEC.md`
    - `.planning/phases/07-usage-analytics/07-USAGE-MAPPING.md`
  </read_first>
  <action>Create `Usage.tsx` as a dedicated route using `PageShell fluid`, History's `DashboardPanel` visual contract per D-02, coss `Tabs`, coss `Select`, coss `Switch` only if `summary.availableFilters.hasSidechain === true`, coss `Table`, lucide icons, and ECharts through the existing `echarts` wrapper/theme tokens. Add `/usage` to `App.tsx`, include it in `LAST_VIEW_ROUTES`, and lazy-load `Usage`. Add sidebar item immediately after History and before Settings using `ChartPieIcon` and `nav.usage`, implementing D-01 and D-03. Implement the page-open sequence explicitly: `Usage.tsx` derives current period/filter args, calls `refreshUsageSummary(args)` first to render the cached DB summary immediately, triggers `runUsageRefresh()` once on mount/open for pricing plus transcript refresh, calls `refreshUsageSummary(args)` again after success to update historical estimates and charts, and leaves the cached summary visible on failure; if no usable price/cache exists, cost fields remain `Unknown` and token totals remain visible. Usage header controls order must be spinner, period tabs, agent/project/model filters, and `Refresh Usage` button. Render these minimum panels from `07-USAGE-MAPPING.md` after D-16 is satisfied: four metric tiles (`estimated cost`, `total tokens`, `cache hit rate`, `top model or agent/model mix`), daily usage trend ECharts panel, token breakdown panel, project/model/agent table, and pricing/status/audit panel. Cost displays `Unknown` when nullable per D-12; token totals remain visible. Do not add Cursor/Gemini labels, legends, filters, or totals. Do not add export buttons, CSV/JSON buttons, or `vibetime usage` references.</action>
  <verify>
    <automated>rtk pnpm --filter @vibetime/desktop typecheck && rtk rg -n "path=\"/usage\"|nav\\.usage|ChartPieIcon|Refresh Usage|Daily usage trend|Token breakdown|Unassigned usage|Unknown" packages/desktop/src/renderer/src/App.tsx packages/desktop/src/renderer/src/components/Sidebar.tsx packages/desktop/src/renderer/src/views/Usage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `rtk rg -n "to: '/history'.*\\n.*to: '/usage'.*\\n.*to: '/settings'" packages/desktop/src/renderer/src/components/Sidebar.tsx` or equivalent file read confirms Usage is directly after History.
    - `rtk rg -n "echarts|useChart|Tabs|Select|Table|Refresh Usage" packages/desktop/src/renderer/src/views/Usage.tsx` finds required UI primitives.
    - `rtk rg -n "Cursor|Gemini|cursor|gemini|CSV|JSON|Export|vibetime usage" packages/desktop/src/renderer/src/views/Usage.tsx packages/desktop/src/renderer/src/components/Sidebar.tsx` returns no Usage UI scope creep.
    - Desktop typecheck passes.
  </acceptance_criteria>
  <done>Usage is a dedicated ECharts/coss dashboard route below History with honest cost/pricing states and no deferred agent/export scope.</done>
</task>

<task type="auto">
  <name>Task 3: Add Settings refresh-frequency control and localized copy</name>
  <files>packages/desktop/src/renderer/src/views/Settings.tsx, packages/desktop/src/renderer/src/locales/en.json, packages/desktop/src/renderer/src/locales/zh.json, packages/desktop/src/renderer/src/views/Usage.tsx</files>
  <read_first>
    - `packages/desktop/src/renderer/src/views/Settings.tsx`
    - `packages/desktop/src/renderer/src/locales/en.json`
    - `packages/desktop/src/renderer/src/locales/zh.json`
    - `packages/desktop/src/renderer/src/views/Usage.tsx`
    - `packages/desktop/src/shared/ipc-types.ts`
    - `.planning/phases/07-usage-analytics/07-UI-SPEC.md`
    - `.agents/skills/coss/SKILL.md`
  </read_first>
  <action>Add all UI-SPEC strings to `en.json` and `zh.json`: `nav.usage`, Usage eyebrow/title, `Refresh Usage`/`刷新用量`, empty state, error state, estimated-cost disclaimer, pricing refresh in progress, cached pricing, pricing unavailable, unknown model price, Claude/Codex-only note, `Usage refresh frequency`/`用量刷新频率`, Settings description, and interval labels for every 15 minutes, every 30 minutes, every hour, and every 4 hours. In `Settings.tsx`, add a Usage settings row under App Startup or a new Usage section using coss `Select`, exact values `15m`, `30m`, `1h`, `4h`, default display from `preferences.usageRefreshFrequency`, and `updateAppPreferences({ usageRefreshFrequency: value })`. In `Usage.tsx`, replace hardcoded visible copy with localized keys. Keep Settings and Usage copy free of Cursor/Gemini and export references.</action>
  <verify>
    <automated>rtk pnpm --filter @vibetime/desktop typecheck && rtk rg -n "usage\\.refreshFrequency|usage\\.refreshFrequencyDescription|usage\\.pricingUnavailable|usage\\.claudeCodexOnly|nav\\.usage|刷新用量|用量刷新频率" packages/desktop/src/renderer/src/views/Settings.tsx packages/desktop/src/renderer/src/views/Usage.tsx packages/desktop/src/renderer/src/locales/en.json packages/desktop/src/renderer/src/locales/zh.json</automated>
  </verify>
  <acceptance_criteria>
    - `rtk rg -n "15m|30m|1h|4h" packages/desktop/src/renderer/src/views/Settings.tsx packages/desktop/src/renderer/src/locales/en.json packages/desktop/src/renderer/src/locales/zh.json` finds all interval values/labels.
    - `rtk rg -n "Cursor|Gemini|CSV|JSON|Export|vibetime usage" packages/desktop/src/renderer/src/views/Usage.tsx packages/desktop/src/renderer/src/views/Settings.tsx packages/desktop/src/renderer/src/locales/en.json packages/desktop/src/renderer/src/locales/zh.json` returns no forbidden scope references.
    - Desktop typecheck passes.
  </acceptance_criteria>
  <done>Usage copy is localized and Settings controls the validated background refresh cadence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer -> preload IPC | UI can only call allowlisted typed IPC channels. |
| Usage summary -> charts | Charts must not display unsupported agent scope or fake costs. |
| Settings UI -> config IPC | Refresh frequency input is user-controlled and must remain enum constrained. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ipc-validation | Tampering | `store.ts`, `Usage.tsx`, `Settings.tsx` | mitigate | Renderer sends only typed enum/filter args; main validation from Plan 05 remains authoritative. |
| T-pricing-integrity | Information Disclosure / Tampering | `Usage.tsx` cost display | mitigate | Nullable cost renders `Unknown` and pricing unavailable copy; never `$0.00` for unknown models. |
| T-attribution-integrity | Tampering | project ranking/audit UI | mitigate | Unassigned usage remains visible in audit panel and is not hidden from totals. |
| T-usage-privacy | Information Disclosure | renderer UI | mitigate | UI consumes aggregate/sanitized IPC data only; no renderer filesystem/SQLite access. |
| T-hook-hot-path | Denial of Service | renderer/settings | accept | This plan touches renderer/config UI only; hook hot-path static checks are covered by Plans 04-05 and final verification. |
</threat_model>

<verification>
Run:
- `rtk pnpm --filter @vibetime/desktop test -- src/renderer/src/usage-store.test.ts`
- `rtk pnpm --filter @vibetime/desktop typecheck`
- `rtk pnpm run ci`
- `test -f .planning/phases/07-usage-analytics/07-USAGE-MAPPING.md`
- `! rtk rg -n "vibetime usage|usage export|exportUsage|Cursor|Gemini" packages/desktop/src/renderer/src/views/Usage.tsx packages/desktop/src/renderer/src/components/Sidebar.tsx packages/desktop/src/renderer/src/locales`
</verification>

<success_criteria>
Plan succeeds when Usage is a separate localized route below History, renderer store tests pass, UI uses ECharts/coss and mapping-approved panels, Settings exposes refresh cadence, and static checks show no Cursor/Gemini usage UI or usage CLI/export surface.
</success_criteria>

<output>
After completion, create `.planning/phases/07-usage-analytics/07-06-SUMMARY.md`.
</output>
